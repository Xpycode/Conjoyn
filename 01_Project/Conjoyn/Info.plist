<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Base Info.plist. Only holds keys the INFOPLIST_KEY_* build settings can't inject;
	     Xcode (GENERATE_INFOPLIST_FILE=YES) merges the generated/standard keys on top at build time.
	     UIDesignRequiresCompatibility opts the app out of macOS 26 (Tahoe) Liquid Glass so native
	     toolbar items render flat instead of in glass capsules. -->
	<key>UIDesignRequiresCompatibility</key>
	<true/>
	<!-- Sparkle auto-update. These SU* keys MUST live here in the base plist, NOT as
	     INFOPLIST_KEY_SU* build settings: with GENERATE_INFOPLIST_FILE=YES, custom SU* keys
	     are silently dropped (not on Apple's allowlist) → runtime "specify the URL of the
	     appcast" (same trap class as cookbook #89). SUPublicEDKey is filled in Wave 0 from the
	     freshly generated EdDSA keypair. -->
	<key>SUFeedURL</key>
	<string>https://conjoyn.lucesumbrarum.com/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>Ks14npeWNt9Rd8QawQiBYQuzFq08vPe2hXgu1s5zVOE=</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<!-- TCC gate for the watch-folder (Wave 5). App Sandbox is OFF, so a security-scoped
	     bookmark is a no-op and cannot grant access to a removable volume — only this usage
	     description + a user consent prompt can. A background watch-folder reading an SD card's
	     DCIM tree triggers the removable-volume TCC prompt; without this key macOS denies the
	     read silently. (memory: sandbox-off-tcc-is-real-gate) -->
	<key>NSRemovableVolumesUsageDescription</key>
	<string>conjoyn watches this volume for DJI drone footage to auto-stitch split recordings.</string>
</dict>
</plist>
