# Session: 2026-06-14

## Goal
Light session — status check + open the app. No feature work.

## Progress
- Ran `/status`: confirmed project is 100% feature-complete, **337 tests / 1 skip / 0 fail**,
  1.0 gated only on **Sparkle Wave 4** (website host standup + publish). The shipped
  `04_Exports/Conjoyn.dmg` lags `main` (predates light theme, single-window mode, diagnostic
  logging, Roadmap topic) — a re-cut is owed as part of the public-release push.
- "Open app" first opened the **stale installed** `/Applications/Conjoyn.app` (notarized build
  100, still `WindowGroup`) — user caught it: they want the **current** version, not a leftover.
- **Established a standing preference + did it right:** killed the stale copy, `xcodegen generate`,
  clean ad-hoc-signed Debug build (`CODE_SIGN_IDENTITY="-"`, this Mac is release-only — Developer
  ID but no *Mac Development* cert) → **BUILD SUCCEEDED** → launched the fresh build from
  DerivedData (`Conjoyn-hapqhbkjvtntrwdowsafhqntcniy/Build/Products/Debug/Conjoyn.app`). This
  build has the single-window `Window` scene, light theme, diagnostic logging, and Roadmap topic.

## Decisions
- **Memory saved (feedback):** `open-app-means-fresh-build` — when the user says "open/run app",
  build current HEAD and launch THAT; never `open -a Conjoyn` on a pre-existing `.app`. The
  installed copy routinely lags `main`.

## Next
- Unchanged from before: **Sparkle Wave 4** (website session) is the only gate to 1.0-public.
- ~~Re-cut `04_Exports/Conjoyn.dmg`~~ **DONE this session (below).**

---

## (cont.) DMG re-cut from current `main`

### Goal
Re-cut the shippable DMG so it matches `main`; also clarified that **Sparkle Wave 4 (website)
is owned by a different repo** (`3-Websites/App-Websites`, where `APPS/Conjoyn/` is "planned, not
yet added") — so within the Conjoyn repo, only the DMG re-cut was actually owed.

### Progress
- **Committed + pushed** the morning status-check session log (`9453694`).
- Re-cut `04_Exports/Conjoyn.dmg` via `make-dmg.sh` → hit and fixed **three fresh-release-Mac snags**:
  1. **`create-dmg` not installed** — `brew install create-dmg` (1.2.3). It's a Homebrew tool, not
     vendored, so it doesn't carry over between Macs. Caught at preflight (no build wasted).
  2. **No `conjoyn-notary` keychain profile** (keychains don't sync). Wrote a reusable helper
     `01_Project/scripts/setup-notary-profile.sh` that auto-reads the `AuthKey_*.p8` + key-id +
     issuer (`IssuerID.rtf`) from `99-AUTH/` and stores the profile. User ran it → Apple-validated.
     (The classifier blocked me from scanning `99-AUTH/` directly — correct; the script is the user's
     own action and prints no secret.)
  3. **Archive failed: `No signing certificate "Mac Development" found`** — this is a release-only Mac
     (Developer ID Application only). Fixed `notarize.sh` to pin the archive to
     `CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY="Developer ID Application" DEVELOPMENT_TEAM=$TEAM_ID`
     (the `-exportArchive` step re-signs all nested code with Developer ID anyway, so the archive's
     identity is throwaway — it just had to *succeed*).
- After the fix the **app half ran clean**: archive → export → **8/8 nested Mach-Os Developer ID +
  hardened runtime** → notary **Accepted** → stapled → `source=Notarized Developer ID`.
- **`create-dmg` then timed out on the Finder AppleScript (`-1712`)** when run from a backgrounded
  shell — Finder can't render the styling window outside a GUI (aqua) session. **Resolved by running
  the DMG step interactively** (`!` prefix) with `SKIP_APP=1` (reuses the already-stapled app → one
  more notary round-trip, no rebuild). Finder styling completed; DMG notary **Accepted**, stapled,
  Gatekeeper-accepted.
- **Verified final artifact:** `04_Exports/Conjoyn.dmg` (27 MB), **1.0 (100)**, stapled,
  `source=Notarized Developer ID`, and `help-roadmap.md` bundled inside → confirms it's current
  `main` (light theme + single-window + diagnostic logging + Roadmap all included).
- Committed the two script improvements (`22373d4`, pushed). The DMG itself stays gitignored.

### Decisions
- **Sparkle Wave 4 is out of the Conjoyn repo's scope** — it's a `3-Websites/App-Websites` session.
  Memory `wave4-lives-in-websites-repo` records this; don't frame Wave 4 as Conjoyn-repo work again.
- **`notarize.sh` archive signs with Developer ID, not automatic/dev signing** — makes the release
  pipeline portable to any Mac holding the Developer ID Application identity, no per-Mac dev cert.
- Memory `dmg-recut-on-fresh-release-mac` captures all three recurring snags + their fixes.

### Next
- **Sparkle Wave 4** (stand up `conjoyn.lucesumbrarum.com`, host `appcast.xml` + this raw DMG,
  publish the link) — in the **App-Websites** project. Last gate to 1.0-public.
- Conjoyn repo: nothing owed. Tree clean, in sync with origin; DMG matches `main`.
