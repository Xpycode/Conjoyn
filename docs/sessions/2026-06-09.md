# Session — 2026-06-09

## Focus
Land 1.8 SpeedTracker (Wave 1 queue port), then put the project under version control
(`git init` + initial commit).

## What happened

### 1.8 SpeedTracker (ported from P2toMXF)
Ported `SpeedTracker` with three DJI adaptations:
- **Drop `processingMode`** — DJIjoiner has a single join mode (concat `-c copy`), so the
  per-mode speed buckets collapse to one.
- **Sum `DJIClip.durationInSeconds` directly** — exact `CMTime` durations, no edit-unit /
  frame-rate math (the P2/MXF path needed that; DJI MP4s carry duration natively).
- **Injectable storage dir** (`init(storageDirectory:)`) — tests hit a temp dir, not the real
  `~/Library/Application Support/DJIjoiner`. App-support folder renamed `P2toMXF` → `DJIjoiner`.
- **Persistence made synchronous** — tiny file, written at job boundaries; removes the
  detached-write lifetime race from the original.

**16 new tests** (estimate tiers, 50-record cap, slow-speed warning, on-disk round-trip).
**121 tests pass** (105 prior + 16).

### Version control
- `git init` — repo on `main`, **no commits yet** at start of the commit step.
- Reviewed the existing `.gitignore` (already thorough: `_reference/`, build artifacts, bundled
  GPL FFmpeg/exiftool binaries, xcodegen-generated `.xcodeproj`, secrets). Added `.fastembed_cache/`
  (tool embedding cache) to it.
- Verified nothing sensitive was staged — no `_reference/`, no binaries, no `.xcodeproj`; largest
  staged file was 32K of docs.
- **Initial commit `011bf19`** — 107 files: model layer, footage-free engine, SRT parser/stitcher,
  queue infra (TempDir/DiskSpace/SpeedTracker), Directions docs, spec, decisions, session logs.

## Decisions
- **Initial commit lives on `main`** (not a feature branch) — it's the repo baseline for an
  already-built, already-tested tree, so there's nothing to branch *from* yet. Feature-branch
  discipline resumes for the next unit of work (1.9 VerificationService).
- **`.swift` sources are the committed truth; `.xcodeproj/` stays gitignored** — `project.yml` +
  xcodegen is the source of truth (avoids pbxproj merge conflicts across the multi-Mac workflow).
  The pbxproj file-ref wiring for sources remains local-only, same as every prior wave.

## Next
- **Branch, then port 1.9 VerificationService** from `_reference/P2toMXF/Services/VerificationService.swift`.
  `VerificationModels` types already exist — focused port: Quick (container + head/tail decode) +
  Full (VideoToolbox full decode) over the joined MP4/MOV, with cancellation + progress.
- Then **1.10 ThumbnailManager → 1.7 QueueManager** → unblocks **3.3 wire SRT into join**.
- Carryover: swap interim GPL FFmpeg → LGPL static before release (6.1).
- Still **blocked on real DJI footage:** 2.2 metadata reader, 2.3 folder reader, 2.4 grouping,
  2.7 TS-remux fallback.
