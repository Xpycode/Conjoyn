# 2026-06-25 — Shipped 1.0.3 / build 103 (public point release)

Continuation of the 2026-06-24 session (crossed midnight during the notary round-trips).

## Why ship
This session's own work (Wave 6.5 `JoinGuardIntegrationTests`, +4) was test/docs only — nothing to
ship. But `main` had accumulated real **production** fixes since the live 1.0.2 DMG:
- **Missing-middle index-gap guard** (`b4ec873`) — a dropped slow-mo middle segment was silently merged
  across the hole → corrupt `-c copy` join. Now splits at the gap. (Data-integrity fix.)
- **Watch-folder hardening** (`2905b38`) — FSEvents teardown use-after-free, hung-discovery deadlock
  (bounded timeout + split latch), swapped-source `(dev,ino)` TOCTOU guard.

User asked "can we ship?" → confirmed this Mac (M4 Pro) holds the Developer ID identity, the
`conjoyn-notary` profile, and the Sparkle key → **full ship including deploy**.

## Steps (all on the M4 Pro)
1. **Version bump** — `project.yml` 1.0.2/102 → **1.0.3/103**, `xcodegen generate`; Release settings confirm.
2. **Release notes** — wrote `04_Exports/appcast/Conjoyn-1.0.3.html` (plain-language, mirrors 1.0.2).
3. **`make-dmg.sh`** — clean Release archive → Developer-ID export (re-signs nested Sparkle/XPC + ffmpeg)
   → verified every nested Mach-O is Developer-ID + hardened-runtime → **app notary round-trip = Accepted**
   → styled DMG → sign → **DMG notary round-trip = Accepted** → stapled; Gatekeeper `accepted`,
   `source=Notarized Developer ID`. App inside = 1.0.3/103.
4. **`make-appcast.sh`** — staged `Conjoyn-1.0.3.dmg` (29575236 bytes), `generate_appcast --account conjoyn`
   (keychain key); appcast keeps **both** 1.0.3 + 1.0.2 items; all fields verified (version 103, length match,
   edSignature present, enclosure URL, releaseNotesLink → Conjoyn-1.0.3.html).
5. **Deploy** — copied into `App-Websites/APPS/Conjoyn/01_Source/`: `Conjoyn-1.0.3.dmg` (Sparkle enclosure),
   `downloads/conjoyn.dmg` (overwrite — human `dl.php` button, stable name), `appcast.xml`, `Conjoyn-1.0.3.html`.
   Dry-ran, then `deploy.sh` (lftp `mirror -R`, **no `--delete`** → 1.0.2 DMG + `counts.json` preserved).

## Verified live (curl)
- `appcast.xml` → 1.0.3/103, valid edSignature, releaseNotesLink, 1.0.2 item retained.
- `Conjoyn-1.0.3.dmg` → HTTP 200, **content-length 29575236 == signed length** (Sparkle sig will verify).
- `Conjoyn-1.0.3.html` → 200. `dl.php?app=conjoyn` → 302 → `downloads/conjoyn.dmg` 200 (serves 1.0.3).
- `Conjoyn-1.0.2.dmg` → still 200 (old enclosure alive). Sparkle now offers 1.0.3 to 1.0.2 installs.

## Commits
- **Conjoyn `main`:** `project.yml` 1.0.3/103 bump + `PROJECT_STATE` + this log.
- **App-Websites `main`:** `appcast.xml` (replaced) + `Conjoyn-1.0.3.html` (new); DMGs gitignored (uploaded
  via lftp, not committed).

## State
- **1.0.3 / 103 LIVE.** 475 / 1 skip / 0 fail. Wave 6 effectively closed.
- Ship recipe captured in `PROJECT_STATE` Infrastructure for next time.
- Optional next: deferred "segment N appears missing" user warning; more camera families; localization.
