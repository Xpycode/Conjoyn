# Session Index

Chronological log of work sessions for DJIjoiner.

| Date | Focus | Outcome |
|------|-------|---------|
| [2026-06-10b](2026-06-10b.md) | **GUI design pass** — port `02_Design/design_handoff_conjoyn/` to SwiftUI | **Design port DONE** (`design/ui-port` → `main`, `5057f14`). Main window restructured from the HSplitView two-pane layout to the designed vertical flow: 52 pt unified custom titlebar (`.hiddenTitleBar`, traffic-light inset, source well + Scan) → discovered-recordings hero (row spec: orange checkbox, split disclosure + monospace segment sublist, **real first-frame thumbnails** via `ThumbnailManager`, en-dash range titles, SPLIT·N/SINGLE badges, tabular meta columns, 12 % `acc2` selection tint; empty state with folder **drag-and-drop**; scanning state) → output bar (dest well + 3 switches + **gear popover** for the off-design engine knobs + Add N to Queue) → queue rows (220 pt name / flex 5 pt bar / status / icon actions) → collapsible console (line tinting, auto-scroll) → footer (Start/Stop swap, overall bar, green all-done). `Theme.swift` = 1:1 `styles.css` token set. Behavior per handoff: Add-to-Queue skips already-queued groups + clears selection; relabeled **"Preserve timecode" → "Timecode from recording time"** (spec Part 2 relabel). Build clean, **195/195 tests**. **User live-tested mid-session on the real card** (110 clips → 74 groups): scan, disclosure, popover, and a real join through the new UI — **1/1 joined, green Done state**; all five design states confirmed by screenshots. **Surfaced:** sizing/position polish wanted vs the prototype; queueing a *single* recording fails (`Need at least two segments to join`) → **new feature request: single-file export** (copy/remux a lone clip to stamp date/TC + carry `.SRT`). Started `feature/rename-tc-disclosure` (research only — resolver/queue APIs mapped, Penumbra `SourceTimecodeReader` identified; no commits). Next: polish pass, single-file export, rename popover + TC disclosure, DMG. |
| [2026-06-10](2026-06-10.md) | **Ship-readiness: sign + notarize (task 6.2)**, then wire in two delivered design artifacts | **Notarization DONE.** Cert/toolchain were ready (`Developer ID Application: GREGOR MÜLLER (FDMSRXXN73)`, self-contained LGPL helpers, inside-out sign phase). Drove a **Developer ID Release build** (Manual identity — Automatic picks the un-notarizable Apple Development cert); app wrapper + both helpers got hardened runtime + secure timestamp, `codesign --verify --deep --strict` passed, `spctl` = "Unnotarized Developer ID" (expected). Auth = **App Store Connect API key** (reused `99-AUTH/AuthKey_6HTCUZ9L7L.p8`, stored in keychain profile `conjoyn-notary`). Wrote one-command **`scripts/notarize.sh`** (build → verify → zip → `notarytool submit --wait` → `stapler staple` → `spctl`); fixed a `grep -q`/`pipefail` SIGPIPE bug that falsely failed the helper check. **First submission Accepted**, stapled, `spctl -a` = `Notarized Developer ID`; **launch-verified** (no Gatekeeper prompt). Recorded the API key into `~/.claude/apple-developer.md`. — **UI design handoff** (`02_Design/Conjoyn.zip`): high-fidelity HTML/React prototype of the single main window across 5 states + authoritative `styles.css` tokens + SwiftUI-oriented README (dark/orange Final-Cut look); committed as the canonical port target, **port deferred to a dedicated design session** (it's a reskin/restructure of the existing `ContentView.swift` — engine already drives every flow). — **App icon** (`conjoinAppIcon.zip`, molten-weld-bead): project had **no asset catalog at all**, so created `01_Project/Conjoyn/Assets.xcassets/AppIcon.appiconset/`, set `ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon`, regenerated via xcodegen. **Verified in Debug build:** `Assets.car` carries all renditions, Info.plist icon keys = `AppIcon`, launches with the icon in the Dock. Merged: `feature/notarize-signing`→`bd96ac3`, `design/ui-handoff`→`1ce8ca9`, `design/app-icon`→`2de92cb`. Next: **GUI design pass** (port the handoff + DMG wrapper) as its own session; polish 6.3 Apple `Keys` atom + doubled variant suffix. |
| [2026-06-09 (s8)](2026-06-09.md) | **Drive the full GUI pipeline on the real card** (the last validation gap) | Ran the whole flow *through the app* on `DJI_001`: scan → **110 segments / 74 groups / 14 splits auto-selected** → batch-join all 14 splits to the external `2CULL-OUT`. **Found + fixed a ship-blocker that only a live GUI run exposes:** the pre-join disk-space preflight reported **"Zero KB free" on any external/secondary APFS volume** (an SD card / external SSD — the *normal* destination), because `DiskSpace.availableCapacity` trusted `volumeAvailableCapacityForImportantUsageKey`, which returns **0 (not nil)** off the boot volume, and the legacy-key fallback only fired on `nil`. Proven empirically (df + a Swift probe: 2CULL importantUsage=0 vs legacy=882 GB). **Fix:** extracted pure `DiskSpace.usableCapacity(importantUsage:legacy:)` that treats `important ≤ 0` as a miss → falls back to legacy raw capacity (+4 regression tests incl. the exact 0→882 GB case); `fix/diskspace-external-volume` → `main`. **Re-ran the batch: 14/14 joined, 0 failed**, **7/7 SRT sidecars stitched** (every recording that had source `.SRT`). `ffprobe` on outputs: single `hevc 3840×2160` stream (mjpeg preview + data tracks dropped), duration exact (e.g. `0009` 3098.72 s = the app's "51:38"), `creation_time` from SRT-first-cue/filename. **SRT stitch:** `0006` = **71,793 cues continuous 00:00:00→00:47:51, 71793×40 ms = 2871.72 s = the MP4 duration to the ms** (offsets correct, no boundary resets) — and it's a **4× slow-mo** recording (payload wall-clock advances ¼ of cue-time), handled correctly. **Timecode (TC-on re-run, after first attempt missed the toggle):** confirmed a real `tmcd` track both **filename-derived** (`0018` → `18:03:02:00`) and **SRT-cue-subsecond-derived** (`0061` → `18:48:43:16`, frame 16 = floor(.64×25)); the load-bearing `-map -0:d` + `-timecode` combo holds through the GUI. Tests **191 → 195**. Parked in `ideas.md`: tagline candidate *"Stitch Split Segments"* and a future **GUI design pass**. Noted cosmetic: output names double the variant suffix (`…_0009_D_D.mp4`). Next: Wave-6 verify-tolerance calibration / Apple `Keys` atom (6.3) / sign+notarize (6.2) / execute the Conjoyn rebrand. |
| [2026-06-09 (s7)](2026-06-09.md) | **Implement + validate task 2.8** — stamp the resolved date + start timecode on joins | Built the s6-decided model. New `RecordingStartResolver` resolves one authoritative recording-start wall-clock per group from the DJI-first chain (**manual override → SRT first-cue → filename datetime → sane embedded `creation_time` → filesystem date**), with a 2010-floor/now+1d sanity gate on the absolute signals and a timezone-safe SRT↔filename mismatch flag. `QueueManager.resolveJoinMetadata` derives **both** the `creation_time` (ISO-8601Z) **and** the `tmcd` start timecode (`HH:MM:SS:FF`, frame = `floor(subsec×fps)`) from that one `Date`, gated by the `fixCreationDate`/`preserveTimecode` toggles + new `dateOverride`. Ported `TimecodeFormatter.wallClockTimecode` + `ISO8601Z` from Penumbra (`nonisolated(unsafe)` for the Swift-6 formatter cache). Replaced the no-op TODO in `processConcatenateJob`; lossless `-c copy` + param guard untouched. **Validated on the real card** (`DJI_001`, split `0008+0009`): the production arg vector stamped `creation_time=2026-05-21T17:53:03Z` + `timecode 19:53:03:11` — derived from the real SRT first cue `19:53:03.448` CEST at 25 fps — with the `djmd`/`dbgi` data + `mjpeg` preview tracks dropped and a fresh `tmcd` created (the `-map -0:d` gotcha confirmed handled; source had no `tmcd`). **+18 tests** (16 unit, 2 real-ffmpeg integration) → **191 total, 0 new failures**. (Pre-existing: 1 `VerificationService` real-decode test fails on `main` too — the bundled-LGPL `-f null` frame-count returns nil; Wave-6 calibration item, not touched here.) `feature/date-tc-stamp` → `main`. Next: drive the SRT-stitch + this date/TC fix through the **GUI** on the real card; then Wave-6 verification-tolerance calibration + the size-changing Apple `Keys` creationdate atom (6.3). |
| [2026-06-09 (s6)](2026-06-09.md) | **Date+timecode stamp model decided** (unblocks task 2.8); no code | Resolved the open question gating 2.8: how the joined file should carry its recording date + timecode. **Surfaced a contradiction** between two 2026-06-07 decisions — "source timecode is authoritative" vs. the research finding that DJI's `tmcd` is almost always `00:00:00:00` and `creation_time` often wrong. So "trust source TC" had nothing real to trust, which is why 2.8 was never wired (the building blocks — `JoinMetadata`, FFmpeg `-timecode`/`-metadata`, the ~300-line `QuickTimeAtomWriter` — all exist but `processConcatenateJob` stamps nothing). **New model (supersedes "TC authoritative"):** a **resolved recording-start wall-clock** is authoritative, and *both* the date atoms and the output `tmcd` track derive from it — never read from the empty source `tmcd`. Priority: **SRT first-cue wall-clock → filename datetime → sane `creation_time` → filesystem date → manual override**; source `tmcd` honoured only if non-zero. **Mined Penumbra** (`~/ProgrammingProjects/1-macOS/Penumbra`) for the proven port source: `DateCorrectionResolver` (source-priority `Resolution`), `TimecodeFormatter.wallClockTimecode` + `ISO8601Z`, `SourceTimecodeReader` (TN2310 tmcd reader), per-item `dateOverride` manual entry, and the load-bearing FFmpeg gotcha (must `-map -0:d` when emitting `-timecode`, else `-c copy` ignores it — Conjoyn's join already does). Logged to `docs/decisions.md` (top) + PROJECT_STATE. Next: implement 2.8 (port a DJI/SRT-first resolver + formatter, populate `JoinMetadata`), then validate on the real card. |
| [2026-06-09 (s5)](2026-06-09.md) | **App naming** — chose a product name to replace the "DJIjoiner" placeholder | Dedicated name-finding session (the branding chore parked in `docs/ideas.md`). Constraints: **fully camera-agnostic** (action cams in scope, not just drones), **coined/evocative** style (also the legally strongest), **full availability vetting** (web + Mac App Store + domain). Generated + vetted candidates with live searches; **killed by collisions:** Weldr (`weldr.dev`), Onecut (App Store video editors), Splyce (≈Splice/GoPro editor), Mendr (photo app), Continua (≈Continuum/Boris FX), Spool (Mac App Store music-video app), Seamr (Seamr Labs + taken domain). **Clean survivors:** Conjoyn, Unsplit (descriptive→weak mark), Reelm (domain likely taken). **Chosen: `Conjoyn`** (coined conjoin+join — zero collisions, trademark-strong, brand-agnostic) · bundle id `com.lucesumbrarum.conjoyn` · tagline *"Split recordings, made whole."* User independently sanity-checked via Kagi (~20 mostly-dictionary results) + Google (mostly split-token "con JOYN") — no real product owns the exact term. Logged to `docs/decisions.md`, idea marked resolved in `docs/ideas.md`, memory `app-name-conjoyn` written. **Rebrand NOT yet executed** (code/repo still "DJIjoiner"); still owe a *formal* clearance (USPTO TESS + EUIPO Class 9 + WHOIS on conjoyn.com/.app) before committing. Open question flagged: project is xcodegen-driven per `project.yml` but session-1 note says hand-managed pbxproj — resolve before the rename. |
| [2026-06-09 (s4)](2026-06-09.md) | **Swap interim GPL FFmpeg → from-source static LGPL** (task 6.1, last licensing blocker) | New reproducible `01_Project/scripts/build-ffmpeg-lgpl.sh` builds `ffmpeg`+`ffprobe` from unmodified FFmpeg 8.1 source: `--enable-static --disable-shared`, **no** `--enable-gpl`/`--enable-nonfree`, `--disable-network --disable-autodetect` (hermetic — won't link stray Homebrew x264/x265). A copy-only joiner needs zero GPL components, so default-LGPL covers everything the app calls (concat `-c copy`, `-map`, faststart, metadata/timecode, ffprobe JSON, mjpeg thumbnails, `-f null` verify). Script **hard-gates** the output (rejects `--enable-gpl`/`--enable-nonfree`/non-arm64). Result: `ffmpeg -L`=LGPL, `otool -L` only `/usr/lib`+Apple frameworks (self-contained), **20 MB each (was 52 MB)**. **Validated on card `DJI_001`:** ffprobe read OK; concat join exact-lossless (426.44 s = 112.28 + 314.16), mjpeg preview dropped, creation_time+timecode stamped. LGPL ACKNOWLEDGEMENTS rewritten; `fetch-ffmpeg.sh` (GPL) demoted to dev fallback. Docs synced (PROJECT_STATE blocker resolved, plan 6.1 done, decisions logged). `feature/ffmpeg-lgpl` (`b808b6b`) → `main` (`b385250`). Next: GUI end-to-end on the card; 6.2 sign+notarize the new helpers. |
| [2026-06-09 (s3)](2026-06-09.md) | **Exploratory research** — multi-brand file-splitting survey (post-DJI roadmap); no code | 4 research agents + web → **`docs/camera-split-research.md`**. Universal split cause = FAT32 4 GB cap (DJI/Autel/Canon split even on exFAT; the 29:59 limit is a separate *stop*, not a split). **Two families: GoPro alone encodes continuity in the filename (`GH01####`/`GH02####`, trailing digits constant); everyone else increments a counter → must group by metadata** — validates our core decision, plus the variant-guard + slow-mo dual-timebase learnings. **Pluggable seam is small:** only a per-brand filename parser (+ optional AVCHD `.MPL` reader) is brand-specific; grouper, **stream-param identity hard gate**, join engine, queue, verify, sidecar stitch all stay shared. **AVCHD is legacy → defer** (also structurally different: `.MTS`/`.MPL` vs MP4). **Product gap confirmed:** auto-detect joiners are GoPro-only, the cross-format joiner (LosslessCut) is manual — nobody does auto multi-brand + lossless + telemetry + watch-folder ingest. Decisions: Phase-2 = modern MP4/MOV family (GoPro first, brings the GPMF telemetry path); stream-param identity is a hard gate before any join. |
| [2026-06-09 (s2)](2026-06-09.md) | **First real DJI card** (`/Volumes/2CULL/2CULL-IN/DJI_001`, 110 clips/444 GB/7 days): build + validate the footage-gated grouping (2.4), validate a real join, add per-group selection | Migrated PROJECT_STATE to the lean digest (187→60). **Keystone finding:** slow-mo clips report container/playback duration ~4× their real wall-clock capture, so the spec's `creation_time + duration ≈ next start` rule silently mis-groups them (saved to memory `slowmo-dual-timebase`). **Built task 2.4 — metadata-continuity grouping:** chain on the **file-size split-cap + real wall-clock start** (slow-mo-proof; robust to filename indices that reset/collide), never on playback duration or filename order. A capped segment continues; the first sub-cap segment ends the recording; `creation_time` adjacency confirms; variant suffix stays the hard no-merge boundary; reuses `StreamParameterGuard.check`. Pure file-free core (`groupMetas`) + `DJIClip` adapter; hardened `totalFileSize` (NSNumber). **9 new tests** with fixtures verbatim from the card (slow-mo/normal chains, 3.41 GB near-cap final, variant boundary, missing-date/param-mismatch defense). Hand-traced over all 110 clips → expected recordings found. **Real join validated** (`0104–0106`, app's exact recipe, bundled ffmpeg 8.1): main video 933.400 s / 23 335 frames = exact lossless sum — **but found a bug:** DJI embeds a low-res mjpeg *preview* stream that `-map 0:v` dragged in as a malformed 3-frame track; **fixed** with `-map 0:v:0`, re-verified single clean stream. **Per-group selection UI:** checkboxes per discovered recording, Add enqueues only selected, **splits pre-selected** (singles unchecked) so a full card doesn't queue ~70 jobs; All/None/Splits + count. App builds/launches (screenshot-verified). **173 tests pass.** Branches `chore/lean-project-state`, `feature/metadata-grouping` (`ef8489c`+`2c97589`→`8bf0567`), `feature/group-selection` (`544fc25`→`0da94de`) all merged to `main`. Next: drive the full GUI pipeline on the card (validate SRT stitch + date fix on real footage). |
| [2026-06-09](2026-06-09.md) | **Wire UI → QueueManager** + first-cut folder discovery (the recommended no-footage next step) | Replaced the Wave-0 placeholder UI with a working app. **`DJIFolderReader`** (first cut of 2.2/2.3/2.4): scans a folder, parses DJI filenames, reads exact `CMTime` duration + `creation_time` (AVFoundation) and copy-relevant stream params (ffprobe), pairs `.SRT`/`.LRF` sidecars, and groups by **camera-variant suffix** (honours the spec's hard no-merge boundary; real `creation_time`-gap grouping stays footage-deferred — only `group(_:)` changes when it lands). **`ConversionViewModel`**: NSOpenPanel source/output pickers, async scan, `ConversionSettings`, `addToQueue` → `QueueManager.addJob` (security-scoped bookmarks via `withBookmarks`). **`ContentView`** rewrite: FCP-dark UI — source + discovered groups, output settings, queue list (per-job progress/retry/remove/reveal), live console, Start/Stop footer; app injects the VM + shared `QueueManager`, forces dark mode. pbxproj: 2 new files registered (hand-managed). **Build clean, app launches & renders (screenshot-verified), 164 tests still pass.** Merged `feature/wire-ui-queue` → `main` (`96879b4`). Next: real DJI footage to exercise the join end-to-end + land real grouping (2.4); footage-gated engine tasks (2.7 TS-remux, 2.8 Apple Keys atom). |
| [2026-06-09](2026-06-09.md) | 1.8 SpeedTracker + 1.9 VerificationService + 1.10 ThumbnailManager + **1.7 QueueManager** ports, then **cleared the stale "app crash" blocker** (it was a pre-fix test crash) and **wired 3.3 SRT into the join pipeline** (Wave 3 SRT COMPLETE, 164 tests, `4b10162`) | Ported `SpeedTracker` (drop `processingMode`, sum `DJIClip` `CMTime` directly, injectable storage dir, synchronous persistence); 16 tests. Ported **1.9 VerificationService** (reuse `VerificationModels`, MP4/MOV framing, Swift 6 `@unchecked Sendable` mirroring `FFmpegWrapper`). **Reconcile:** a prior draft of this log claimed a `git init` + initial commit `011bf19` (107 files) + a `feature/wave1-verification` branch off `1acaa25` — **none existed** (repo versioned since `797d313` on 2026-06-07; the source sat uncommitted). Corrected: gitignored tool caches (`c49ddd3`), merged SpeedTracker via `feature/wave1-speedtracker` → `main` (`609d8f4`), committed WIP 1.9 (`8bdc140`). Then **finished 1.9**: implemented `passesFrameCheck` (5% frame-shortfall tolerance, over-count passes, no-estimate passes iff ≥1 frame; constant flagged for Wave 6 footage calibration) + 14 tests (incl. a real-ffmpeg end-to-end that ran, 0 skips), merged `--no-ff` to `main` (`4b0e8a6`). **135 tests pass.** Then **ported 1.10 ThumbnailManager** + `FFmpegWrapper+Thumbnails`: LRU cache / concurrency semaphore / cancellation port verbatim; DJI drops the P2 PROXY→ICON→MXF fallback chain (single self-contained MP4 → extract from `videoURL`). Added `DJIClip.lastFrameSeekSeconds` (~2-frame/50 ms back-off so the ms-rounded seek can't land past EOF) + reusable `VideoStreamParams.framesPerSecond`. 11 tests incl. a real-ffmpeg first/last extraction; merged `--no-ff` (`3afbae3`). **146 tests pass.** Next: **1.7 QueueManager** (last big Wave 1 port; unblocks 3.3 wire SRT into join). |
| [2026-06-08](2026-06-08.md) | Plan **then build** Wave 1.2/1.3 — the `DJIClip` model layer | Research fan-out (2 Explore agents on the P2toMXF model layer + its consumers, apple-doc MCP on CMTime, web on DJI MP4 metadata) → an 8-file plan, **1.3 DJI models before 1.2 ports**. Then implemented it: `DJIClip` (Int64+Int32 duration → exact `CMTime`; embeds `SegmentStreamInfo?`; `from(parsed:)` factory), `ConversionSettings` (lean + `OutputContainer{.mp4,.mov}`), `RecordGroup`, `DJIFolder`, `ConversionJob`+`JobStatus` (one job = one record-group), `VerificationModels`, `ProgressModels` estimation types; `StreamParameterGuard` param structs made `Hashable, Codable, Sendable`. Keystone full-`ConversionJob` Codable round-trip green. **105 tests pass** (91 prior + 14 new), merged `--no-ff` to `main` (`fa468af`). Next: 1.8 SpeedTracker → 1.9 → 1.10 → 1.7 QueueManager. |
| [2026-06-07](2026-06-07.md) | Setup + plan, then build (Wave 0 + Wave 1 slice + Wave 2 slice) | Directions, spec + 10 decisions, P2toMXF cloned, 4-agent research, IMPLEMENTATION_PLAN.md (7 waves). Then: Wave 0 scaffold + signed interim-GPL FFmpeg 8.1 (merged), Wave 1 vertical slice ported (11 tests, merged), Wave 2 (2.1 DJIFilenameParser + 2.5 concat arg-builder + 2.6 stream-param guard + 2.8 native date-atom writer, **50 tests**, merged `9590598`). Then Wave 3.1 `SRTParser` (tolerant 3-layout SubRip parser, verbatim payload + wall-clock, 20 tests, merged `563e330`) and Wave 3.2 `SRTStitcher` (offset = Σ decoded `format=duration`, global renumber, missing-sidecar still advances, 11 tests incl. real-ffprobe end-to-end, **81 pass**, merged `d60d30b`). Then began Wave 1 queue ports: mapped the ~2,750-line reference surface, found SpeedTracker/QueueManager need the DJIClip model layer (so **decided to define DJIClip now from spec**), and ported 1.5 `TempDirectoryManager`+`DiskSpace` (model-agnostic, 10 tests, **91 pass**, merged `6f58896`). Next: 1.2/1.3 models → 1.8 SpeedTracker → 1.9 VerificationService → 1.10 ThumbnailManager → 1.7 QueueManager. |
