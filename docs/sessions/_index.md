# Session Index

Chronological log of work sessions for DJIjoiner.

| Date | Focus | Outcome |
|------|-------|---------|
| [2026-06-24 (variant+codec)](2026-06-24.md) | **Wave 6.5 variant + mixed-codec closed via synthetic real-tool fixtures** | Continued Wave 6 (prior session's Resume option a). Real DJI multi-lens split footage is **undownloadable** + Mini 4 Pro is single-lens, so instead of leaving the two guards proven only on hand-built params, drove them through the **production** path on real files. **First found:** all three 6.5 guards were already *unit-tested*; the only gap was end-to-end. The existing real-probe test (`testEnsureJoinableAgainstRealProbe`) only varied **resolution** — codec + fps were the un-covered cases. **Bundled FFmpeg is LGPL (no x264/x265)** → can't encode h264/hevc; used native `mpeg4` vs `mjpeg` (guard compares codec-name *strings*, identical path). **`group()` hardcodes a 3 GB cap floor** → tiny synthetic clips would be vacuous singles, so drove `groupMetas` directly with a lowered floor so same-lens clips actually chain. **Built** `ConjoynTests/JoinGuardIntegrationTests.swift` (+4): `mpeg4`+`mjpeg` and 25-vs-30 fps **refused by `ensureJoinable`** end-to-end (real ffprobe), a positive control, and `_W`/`_T` clips parsed by the real `DJIFilenameParser` that never co-group even when size/time/index would chain them (asserts exactly 2 groups → non-vacuous). `xcodegen generate` (new file) → **475/1 skip/0 fail (+4), no regressions, no production code changed.** **Still footage-gated (can't synthesize):** real multi-lens *index numbering* + exact h264/hevc bytes. Wave 6 effectively closed. `decisions.md` + plan 6.5 row + `PROJECT_STATE` updated. **Uncommitted, Debug-local** — awaiting go-ahead; shipped 1.0.2/102 untouched. |
| [2026-06-24 (missing-middle)](2026-06-24.md) | **Wave 6.5 missing-middle: found & fixed a slow-mo silent-merge (index-gap guard)** | User asked me to **source multi-lens / mixed-codec / missing-segment footage** off the web (they only have the single-cam M4P). **Web verdict (honest):** real DJI **multi-lens *split-video* + original filenames + SRT is essentially undownloadable** — single edited clips / DNG stills / R-JPEG thermal / extracted frame-sequences only; even dedicated DJI-telemetry GitHub repos ship **SRT-only fixtures** (verified the trees). Reframed into **verifying the 3 guards fire** → grounded in code: variant-guard + codec-guard **already unit-tested**; **missing-middle untested**. **Caught an Explore agent's wrong conclusion** by reading `continues()` myself — a missing *normal-speed* segment **splits** (deleted segment's own time widens the wall-clock gap past `playback+12 s`). **But found the real hole:** the bound uses **playback** duration, ≈4× real for slow-mo, so a missing *slow-mo* segment (~398 s real gap < ~806 s bound) **silently merges across the hole** → corrupt `-c copy` join (~3.3-min discontinuity + SRT misaligned), no warning. **Proven with 2 characterization tests** on the file's real 100 fps numbers. **Fix (user chose "index-gap guard"):** `continues()` **step 3** — adjacent segments must be **index-consecutive** within their variant bucket (negative signal only; only ever *adds* a split, never a merge → can't corrupt a good group). **+3 tests, 471/1 skip/0 fail (+3), no regressions.** Committed `b4ec873` → `--no-ff` `cd001bd`; `decisions.md` logged. **Closes 6.5 missing-middle; variant-guard + mixed-codec still footage-gated** (web footage doesn't exist → synthetic/renamed M4P fixtures the cheaper path). **Pushed — `main` synced** (incl. docs `e9b6e04`). Shipped 1.0.2/102 untouched. |
| [2026-06-24 (earlier)](2026-06-24.md) | **Wave 6.3 + 6.4 validated on real timestamped slow-mo footage (M4P-1, DJI Mini 4 Pro)** | Prompted by the user's push-back ("didn't we validate this on 2CULL already?") — true for **legacy** naming, but 2CULL never had the **timestamped** scheme or **slow-mo**. M4P-1 has both. **Grouping proven semantically correct** (not just self-consistent): hand-traced `continues()` → 6 groups from 9 clips, the one 4-segment merge (0006→0009) verified against the SRT's **own wall-clock** — seams exact to the second; 0010 correctly kept separate (74 s real gap). **6.3 lossless join** (app's exact arg vector, bundled FFmpeg, read-card/write-off-card): duration = **exact Σ 2871.72 s**, streams **byte-identical** (10-bit HDR `Main 10` preserved), **full decode-to-null clean (exit 0, 0 errors)** + 3/3 seam windows clean; `creation_time`+`tmcd` write-back confirmed (resolver picks `.srtFirstCue`, 3 signals agree). **6.4 SRT alignment**: max seam drift **+34 ms < 1 cue (40 ms), no accumulation**. **Slow-mo (≈4×, 100→25 fps) — first ever seen on a card** — validates the chain-on-cap-not-playback design. Flipped **6.3 ✅ / 6.4 ✅** in plan; **6.5 footage-gated** (single-camera Mini 4 Pro can't exercise variant-guard — needs a Mavic 3 Pro / mixed-codec / deleted-middle set). **Docs-only; no code touched; tests unchanged; shipped 1.0.2/102 untouched.** |
| [2026-06-24 (later)](2026-06-24.md) | **Watch-folder daemon hardening — 3 deferred engine-review items fixed + merged** | Closed the hang/UAF/TOCTOU edge cases from the 2026-06-23 review on `fix/wave5-watchfolder-hardening` → `main` (`--no-ff` `2905b38`, branch deleted). **#1 hung-discover** (`e3f9789`): a wedged ffprobe latched `isRescanning` forever (the `defer` never fires on a coroutine suspended at `await`) → split into `isDiscovering`/`isResampling` + bounded `withDiscoverTimeout` (new tunable `WatchFolderSettings.discoverTimeout`, default 90 s; wedged task **abandoned not awaited** since ffprobe ignores cancellation; `ResumeGate` actor + `withCheckedContinuation` race) + **#4** stale-key cache eviction. **#2 FSEvents UAF** (`d7e05fe`): stream now takes a **context retain** on the monitor (balanced at `FSEventStreamRelease` after `Invalidate`) — intentional stream↔monitor cycle broken by the explicit `stop()` the coordinator always calls; `deinit` demoted to fallback. Learned: `FSEventStreamRelease` runs the release cb **async** on the dispatch queue, so lifetime tests must *wait* for dealloc. **#3 enqueue→join TOCTOU** (`3ee5933`, cookbook #127): `FileIdentity` `(dev,ino)` `lstat` snapshot at enqueue (in the shared `addJob` funnel) re-verified before `mergeClips`; swap/rotation throws non-retriable `FFmpegError.sourceIdentityChanged`; **policy divergence** — `.unverifiable` does NOT block (read-and-produce, unlike #127's trash) ; restored job = no baseline = no-op. **468/1 skip/0 fail (+13).** User chose "timeout + keep cadence" for #1. `decisions.md` + `PROJECT_STATE` updated. Shipped 1.0.2/102 untouched. |
| [2026-06-24](2026-06-24.md) | **Real removable-SD-card eyeball (5.14) → Wave 5 CLOSED** | **PASSED on a genuinely removable built-in-SDXC card** (`diskutil` `Removable Media: Removable` — the precondition `2CULL`/Fixed failed). `/status` first reconciled a byte-identical Syncthing phantom (`git reset --hard origin/main`, FF `cd86097`, no dup commits). Clean Debug build → watched `/Volumes/M4P-1`: **6 DJI groups** joined `-c copy` + SRT-stitched + verified **off-card** (incl. a 4-segment **13.46 GB** stitch), originals untouched, 0 failures. **TCC finding:** the `NSRemovableVolumesUsageDescription` prompt **never fired** — picking via `NSOpenPanel` is the **powerbox** (user-consent grant, no prompt even on removable media), and it **persists across relaunch**. **Relaunch:** entry + plain bookmark + `ProcessedGroupLedger` all survived → watch resumed with **no re-prompt, no re-join** (ledger held; log grew 0 lines, 0 `EPERM`). **Live drop-test (decisive):** a freshly-copied clip (new index) was **auto-detected in ~25 s and joined via pure background card access** (17.4× realtime) — proves the relaunched watcher is genuinely live, not just UI-restored, and rules out "silent watcher death" on the happy path. Date/TC discrepancy handler also fired correctly. Honest caveat: the dialog never visually fired, so "powerbox-persisted" vs "built-in slot not TCC-gated like a USB reader" is indistinguishable here — practical outcome identical, USB SD reader untested. Cleaned up all test artifacts (~32 GB freed, watch entry removed). Flipped **5.14 ✅ / Wave 5E ✅** in the plan; updated PROJECT_STATE. **No app code changed; 455/1 skip/0 fail unchanged; shipped 1.0.2/102 untouched.** Does **not** retire the 3 deferred hardening items (hang/UAF/TOCTOU). |
| [2026-06-23](2026-06-23.md) | **`/arrive` Syncthing reconcile (stale wave-5 branch) + post-hoc Wave 5 engine review** | **No code changed.** This Mac was stranded on the already-merged-and-deleted `feature/wave5-watch-folder` (`e3b5c3e`); its "uncommitted changes" were a **pure Syncthing shadow** — all **13** files byte-identical to `origin/main`. Reconciled per `37_multi-mac-discipline.md` Rule 1 (restore + rm shadow → `checkout main` → **FF 16 commits `fbd08b8`→`5cec1ee`** → `branch -d`), **no duplicate commits**. Orphaned tip `e3b5c3e` (cookbook #126/#127/#61 sync into Conjoyn's `docs/cookbook` copy) confirmed safe: canonical patterns committed in DIRECTIONS master (`e597bf1`) + remote branch still pins it. Then a **post-hoc deep review of the Wave 5 watch-folder engine** (2 `code-reviewer` agents, every load-bearing claim hand-verified): **9 findings + 1 false positive caught**. Worth-fixing tier — **(1) hung `discover` deadlocks the watcher** (ffprobe hang ⇒ `isRescanning` never clears ⇒ silent death), **(2) FSEvents teardown UAF race** (`passUnretained`+`Invalidate`-from-other-thread; close with retain/release context cbs), **(3) TOCTOU enqueue→FFmpeg** (cookbook #127; capture+re-verify inode). Lower: stale-key leaks in `sampleHistory`/`groupLastChanged`, no `size>0` stability guard (likely unreachable), unbounded ledger, `nil`-vs-`""` fingerprint collision, decorative `WatchGroupState`, shared GCD queue label. **Verified solid:** Swift 6 isolation, the re-join-forever ledger fix, FSEvents lifecycle, Codable fallbacks. **False positive:** "FSEvents rescans queue up" — `isRescanning` is set synchronously before the first `await`, so concurrent tasks correctly drop. Fixes **deferred** → proposed `fix/wave5-watchfolder-hardening`. 455/1 skip/0 fail (unchanged); shipped 1.0.2/102 untouched. |
| [2026-06-22](2026-06-22.md) | **Docs: slim `PROJECT_STATE.md` to the lean digest** | Trimmed `PROJECT_STATE.md` **276 → 86 lines** (back under its own "<100" target) via the Directions `MIGRATE-PROJECT-STATE.md` procedure. Verified **no data loss first**: every decision-bearing item already lives in `decisions.md` (current, incl. 2026-06-20 5D + overlap policy), so the trim is pure de-duplication, not deletion. **Now** → 4 one-liners; **Recent** → 5 plain lines; **Backlog** stripped of ✓DONE walls; added a compact **Infrastructure** block (version/DMG/notary/Sparkle-key/appcast) so shipping-time facts aren't buried; fixed a stale `IMPLEMENTATION_PLAN.md` path (repo root, not `docs/plans/`). No code/tests/build touched (455/1 skip/0 fail). ⚠️ Tree carried in **unrelated cookbook edits via Syncthing** (`PATTERNS-COOKBOOK.md`, `cookbook/61`, new `126`/`127`) — not this session's work, left for the authoring Mac. Nothing committed (none requested). |
| [2026-06-20](2026-06-20.md) | **Wave 5D watch-folder UI — eyeballed on real footage, overlap policy authored, committed + pushed** | **Closed out the multi-folder watch UI the 2026-06-18 session left uncommitted.** Clean build (`xcodegen` first so the 3 untracked Swift files land in the target) → launched fresh (mtime+PID verified). **Real-footage eyeball PASSED** on the external `2CULL` card: *Watch Folder ▸ Watch Folders…* → Add Folder → `WATCHING`, settings popover shows live knobs (quiet 45 s / stable polls 3 / poll 0.75 s / split 3.9 GB); state machine fires live `SETTLING n`→`QUEUED n`→joined into the folder's **own** output; **two folders concurrently** (WATCH3 `QUEUED 9` + WATCH2 `SETTLING 26`) each with its own output dir — the per-folder isolated-ledger design holds; a clean run reads `5 of 26 joined · 0 failed`. **"14 failed" investigated → NOT a watch bug:** the diagnostic log showed all 14 failing in a 0.4 s burst at launch (`ffprobe exit 1` reading **source** files) = the "Restored 14 jobs from last session" set whose source card was unmounted; pre-existing queue-restore behavior, unrelated to Wave 5 (a hand-cancel records as `.cancelled`/amber, not `FAILED`). **Overlap policy authored** (`WatchFolderManager.rejectionReason`, `TODO(user)` retired) — user chose **block on last-known path**: an existing entry whose volume is **offline** (bookmark won't resolve) STILL blocks an overlapping add by falling back to its persisted `rootPath` (`entry.resolvedRootURL ?? URL(fileURLWithPath: entry.rootPath)`), so a re-mount can't silently resurrect a double join; `rootPath` was already stored for exactly this. **+1 regression test** `testRejectionReasonBlocksOfflineEntryViaLastKnownPath` (garbage bookmark → `resolvedRootURL == nil` asserted as precondition, then same/nested reject + disjoint allow). **Full suite 455/1 skip/0 fail (+1).** Committed Wave 5D code + a docs commit to `feature/wave5-watch-folder`, **pushed**, then **merged `--no-ff` to `main`** (`c814efc`, brought all of Wave 5 — engine+UI) + pushed; **feature branch deleted** (local + origin). **Wave 5E closeout (docs):** 5.13 suite green ✅, 5.15 ✅ (logged the 5D + overlap-policy decision in `decisions.md`, moved watch-folder out of PROJECT_STATE "Roadmap futures", marked 5D ✅/5E 🟡 in the plan). **Only 5.14 remains** — the **real-SD-card TCC prompt + relaunch-persistence eyeball**: the `2CULL` join eyeball does NOT cover it because `diskutil` shows `2CULL` is `Removable Media: Fixed` (external USB disk, not a card) → never trips `NSRemovableVolumesUsageDescription`; **user brings a real removable SD card next session** (Resume block in the day log has the steps). Debug-local; shipped **1.0.2/102** untouched. |
| [2026-06-18](2026-06-18.md) | **Post-ship UI-honesty polish (full day)** — thorough-verify button · progress-bar lifecycle · byte-level move progress · **green-only-when-verified (text+footer) · verify folded into one bar** (committed `e90f838` + pushed) | **AM/move-progress run, then a color/bar run — five fixes total, all eyeballed on `2CULL`.** **(4) Green leaked into the *verifying* phase** (status text + footer were still keyed on `job.status`, so a joined-but-verifying file read green in two places before verification): root fix = new **`VerificationStatus.outcomeTier`** (`verified`/`working`/`failed`) as the **single source of truth** — `barFill`, `statusColor`, footer all route through it; `statusColor` no longer greens on `.completed` (verifying → amber `acc1`); footer splits into `verifiedCount` (green) / `awaitingVerificationCount` (orange `acc2`, matches the bar) / `verifyFailedCount` (red). User chose "amber until verified" over a 4th hue. **(5) Verify folded into the single bar** — new **`ConversionJob.lifecycleFraction`** (+`producePortion=0.85`): produce (join+move via `progress`) fills `[0,0.85]`, verify (`verificationProgress`) fills `[0.85,1]`, produce **caps at 0.85** so the hand-off is continuous (no backward jump; safe because `.completed`→`.verifying` is one synchronous main-actor stretch). Lives on the **model** → unit-testable; expanded detail keeps its own fine-grained verify bar (user: "fold in but keep detail"). **Weighting:** rejected equal thirds (move=0 when unstaged, fast verify=seconds → would lurch) for cost-weighting + phase-by-label. **+2 tests → QueueManagerTests 48/48 (full app suite ~354, not yet re-run end-to-end).** `NSOpenPanel` "felt faster" = OS XPC-service warming, **not** these changes. Full suite **354/1 skip/0 fail**; committed **`e90f838`** (code+tests) + docs commit, **pushed**; shipped **1.0.2/102** untouched. **— Earlier the same day (move-progress run): (1) Thorough-verify control** read as plain text — was `.cjGhost` (transparent, no rest-state affordance); switched to **`.cjStandard`** filled button + `checkmark.shield` `Label` icon (dropped a dead `.font(size:11)` — `CJButtonStyle` hardcodes its label font). Traced its action end-to-end: `verifyJobThorough` (sync shim) → `runThoroughVerify` re-opens the lapsed security-scoped **source** access (`.newlyGranted`/`.alreadyActive` `defer` serves both manual + auto-escalation paths) → `verifyThorough` Tier 0 readability / Tier 1 packet-count+duration / Tier 2 byte-exact end-to-end hash; a passing Tier-2 hash = `.verified` regardless of Tier-1 container warnings. **(2) Bar lifecycle — two symptoms, one root cause:** colour+label were driven only by `job.status` (`.active`/`.completed`/`.failed`), collapsing the real five-phase lifecycle (join → staged-move → stitch → verify → done) → bar sat **full under "Joining…"** during the un-tracked move tail, and turned **green the instant the file was written**, before verification. Fixes: new transient **`ConversionJob.isFinishing`** (out of `CodingKeys`, mirrors `isDeepVerifying`) set after `mergeClips` returns + cleared on terminal states → `statusText` shows **"Finishing…"**; **`barFill` now gates green on `verificationStatus == .verified`** (verifying/unverified → orange, verify-failed/status-failed → red). Principle: *green = verified byte-for-byte, never "the file exists".* **(3) Byte-level move progress:** `moveIntoPlace` rewritten from `FileManager.moveItem` (no progress) to a **streamed 8 MB-chunk copy** reporting `progress` 0…1, `fsync` **before** deleting the source (so a mid-copy failure throws with `src` intact + the caller's `destinationTouched` cleanup deletes the partial `dest`; original DJI segments never touched); falls back to atomic `moveItem` if source size unreadable. Composite bar: `joinPortion = staged ? 0.5 : 1.0` → ffmpeg join fills `[0, 0.5]`, the cross-volume move `[0.5, 1.0]` (50/50 = same bytes over the same external bottleneck, principled not tuned); unstaged jobs have no move so the join fills the whole bar. Side benefit: the active-job ETA now extrapolates over join **+** move. **+1 test** (`testMoveIntoPlaceStreamsProgressAndCopiesBytesIntact`, 20 MB multi-chunk: bytes intact, progress monotonic → 1.0). **352 tests/1 skip/0 fail.** Fresh ad-hoc Debug build relaunched after each change (mtime+PID verified). **4 source files + 1 test UNCOMMITTED on `main`;** shipped **1.0.2/102** DMG + appcast untouched. *Next:* real-footage eyeball of the move-progress climb, then one commit to `main`. **— Part 2 (PM): Timecode write-back verification** (`8c4c181`): the headline date/TC fix was *stamped but never verified* → new persisted `ConversionJob.appliedTimecode` + `VerificationCheck.Kind.timecodeWriteback` (runs in fast **and** thorough tiers; pure separator-insensitive drop-frame-aware `compareTimecode`; mismatch/missing=`.fail`, unparseable=`.warning`); `mapStatus` fix so a passing byte-exact hash no longer masks a TC fail. **Live `2CULL` eyeball caught a read-path bug the units couldn't:** tmcd written fine but AVFoundation `SourceTimecodeReader` threw `.missingFormatDescription` (ffmpeg-muxed tmcd attaches its format desc to the *track*, not the sample buffer) → switched verifier to read via **ffprobe** `stream_tags=timecode`; re-run "match" ×5, "No issues flagged" ✓. **+6 tests → 360/1 skip/0 fail.** Zero UI work. Debug-local. **— Part 3 (PM-2): verified + corrected the Wave 5 watch-folder plan** (no app code; `IMPLEMENTATION_PLAN.md` only, `49853a5`). Fanned out 3 agents (2 `Explore` codebase + 1 web/Apple-docs). **6 claims VERIFIED** (dedup-unfinished `ConversionViewModel.swift:351` → re-join risk real; serial loop `QueueManager+Processing.swift:26`; `addJob`/`startQueue`; one-group-one-job; `withBookmarks:314`; stale `RecordGroup.swift:10`). **Corrected 3:** ❌ `DJIFolderReader.read` returns `Discovery` not `[RecordGroup]`; ⚠️ `startAccessingIfNeeded` only starts access (resolution is in `loadQueue`); ⚠️ **big one** — App Sandbox is **disabled** so `.withSecurityScope` is a no-op, plain `bookmarkData()` suffices and the real new gate is **TCC** (`NSRemovableVolumesUsageDescription`) for background SD-card reads. Plus FSEvents latency-is-the-debounce + `DispatchSource O_EVTONLY` non-recursive + atomic-rename/growing-file caveats folded in. New memory `sandbox-off-tcc-is-real-gate`. **— Part 4 (PM-3): `/execute wave 5` — built the watch-folder ENGINE** on branch `feature/wave5-watch-folder` (engine-only, no UI; shipped 1.0.2/102 untouched). **5A** `3478261` (FileStabilityGate · CompleteSetGate · WatchGroupState · ProcessedGroupLedger SHA-256 fingerprint over `stem\|index\|variant`, not the per-parse UUID · stale-comment fix, +50). **5B** `87e5de1` (FSEvents `WatchFolder` · plain-bookmark `WatchFolderBookmark` · `WatchFolderSettings` · Info.plist `NSRemovableVolumesUsageDescription` TCC gate, +15). **5C** `aa010fb` (pure `WatchFolderReconciler` + thin `@MainActor WatchFolderCoordinator` + relaunch resume, +21). **Full suite 446/1 skip/0 fail (+86).** **Two review-caught bugs fixed + regression-tested:** (1) dedup set sourced from the persisted ledger, not an empty-at-launch mirror (else a joined group whose clips stay on the card re-enqueues forever after relaunch); (2) split FSEvents-rediscover from cheap poll re-sample (no ffprobe every 0.75s while idle). **5D UI designed + APPROVED + DEFERRED** (user: don't build this session): new "Watch Folder" `CommandMenu`, footer status readout, its **own** output-folder picker (replaces v1 next-to-source placeholder). 5E real-footage + SD-card TCC eyeball follows 5D. **Branch not merged, not pushed at depart.** **— Part 5 (M4-Pro, later): `/arrive` reconciled a Syncthing split-brain (56 behind + dirty tree = benign file-echo of the already-pushed branch; FF `main`→`fbd08b8`, no dup commits), then BUILT Wave 5D as a multi-folder list window** — engine extension (`WatchFolderCoordinator.outputFolderURL`) + new `WatchFolderEntry`/`WatchFolderManager` (one isolated coordinator per folder) + `WatchFoldersPanel` window + "Watch Folder" menu. **454/1 skip/0 fail (+8); build green; 7 files UNCOMMITTED on the branch.** Overlap policy scaffolded `TODO(user)`. **Next session = GUI/real-footage eyeball, then commit.** |
| [2026-06-17](2026-06-17.md) | **SHIPPED Sparkle Wave 4 → 1.0-public is LIVE** | **The last gate is closed.** Git reconcile first = no-op (pre-flight's "1 unpushed" was a stale pre-fetch snapshot; `main == origin/main`, clean). Found the website landing page + human download **already live**; the lone gap was `appcast.xml`. Also found the live download DMG **stale** (cut Jun 16 12:09, but PM-2 fixes landed 23:44 same day) → user chose **re-cut from current `main`**. **(1)** Bumped `1.0.1/101 → 1.0.2/102` in `project.yml` (`54f69b3`). **(2)** Re-cut notarized DMG on the **M1 Max** (`make-dmg.sh`): notary **Accepted**, double-stapled, Gatekeeper `source=Notarized Developer ID`, version inside 1.0.2/102, 29,487,589 B. **(3)** Signed `appcast.xml` + `Conjoyn-1.0.2.html` notes — EdDSA key not in this Mac's keychain (custody=M4 Pro), so enhanced `make-appcast.sh` with a **`SPARKLE_ED_KEY_FILE`** env that signs via `--ed-key-file` from the `99-AUTH/` backup without touching the keychain (`e844259`). Notes emit as `sparkle:releaseNotesLink` (not inlined) → HTML hosted too. **(4)** Deployed to Strato from the App-Websites repo (`deploy.sh` = `lftp mirror -R` **no `--delete`** → `counts.json` safe → 644): root `appcast.xml`/raw `Conjoyn-1.0.2.dmg`/`Conjoyn-1.0.2.html` + refreshed counted `downloads/conjoyn.dmg`; gitignored root `*.dmg` (`4c1f852`). **(5) Verified live (headless):** appcast/enclosure/notes all HTTP 200; enclosure `Content-Length` == appcast `length` (29,487,589); **`sign_update --verify` on the served bytes = exit 0**; `dl.php` 302 → 1.0.2. (Full GUI Check-for-Updates click-through optional — mechanism proven end-to-end in Wave 3, 100→101.) PROJECT_STATE + memory `wave4-lives-in-websites-repo` updated to DONE (`a191358`). ⚠️ The shell's git tooling **semantically split commits** in the website repo — a pre-existing `apps.json` edit shipped as its own `c236287` ("add Magpie to roster"); revert if unintended. **— PM session 2: post-ship eyeball → ETA + console fixes (UNCOMMITTED) —** Live GUI self-update **confirmed** ("You're up to date! 1.0.2"). A real 60-job 4K eyeball surfaced three things, all fixed: **(1) whole-queue ETA oscillation** (footer swung ~3 min ↔ ~3h 43m as jobs crossed into *Verifying…*) — `remainingQueueSeconds` derived a *live* throughput off the active job's `progress`, which covers only the fast ffmpeg-join phase; the PM-2 staged-move+verify tail isn't progress-tracked, so the sample read ~10× fast mid-join then collapsed during verify → dropped the live override (+regression test, pending estimate now phase-independent). **(2) ETA still felt wrong** because flat steady-state history (70 MB/s) ignores a cold start (log proved first file = 5.9 min/1.7× realtime, 10× slower; run ≈ 13 min vs ~10 est) → per user's idea, ETA now **extrapolates from this run's observed pace** (`QueueManager.sessionBytesDone/sessionSecondsDone`, reset per batch; pending = bytes ÷ session-pace → history → default; +test). **(3) "opening the console hangs the app"** (Conjoyn pegged 99% CPU) — PM-2's single-`AttributedString` `.textSelection` `Text` re-laid-out all up-to-5 000 lines per streamed line; now renders only the **last 300** (Copy All still full). Also diagnosed the "slow folder scan" as **not a bug** (serial per-clip AVAsset metadata, ~28 s cold for 110 clips, machine under-used — candidate for bounded-concurrency parallelization, cookbook #35). **351 tests/1 skip/0 fail; 3 source files + 2 tests UNCOMMITTED; `main` + shipped 1.0.2 DMG untouched.** *Next:* live-queue eyeball the two ETA/console fixes, then one commit to `main`. **— Session 3 (evening) —** **(1)** Console reverted to **line-by-line, uncapped** in a `LazyVStack` (lazy layout bounds cost to visible rows → no hang, full scrollback; per-line selection, Copy All full) — supersedes PM-2's 300-cap. **(2) Stale-build false alarm** (big time-sink, no real bug): user's "regressions" (New Window/tabs back, File-menu items + Copy All gone) were because the *running* app was `04_Exports/_run/Conjoyn.app`, a **Jun 13 / 1.0.1 snapshot synced from a release Mac** (`04_Exports/` is in Syncthing; `.git` isn't → binaries travel, history doesn't). Proven via `ps`+`strings`+`git log`. Earlier PM-2 "verification" was thus against the wrong binary. Built a fresh signed **Release** locally (this Mac has **Developer ID Application** but no Mac Development cert → `CODE_SIGN_STYLE=Manual` + `--options runtime`; signs bundled ffmpeg → runs), overwrote `_run`. *Lesson:* **Swift small-string inlining** (≤15 UTF-8 bytes) hides short literals from `strings` — verify builds with a long literal + binary mtime. **(3) Segmented footer bar** (`381ff89`, committed+pushed, eyeballed): Stop-with-incomplete showed green "✓ N joined, 0 failed" + full green bar (cancelled jobs count as `isFinished` → `overallProgress`=1.0 + success branch). New `cancelledCount` + `CJBarSegment`/`CJQueueOutcomeBar` composition bar (green/red/amber/orange/empty); green ✓ only when `failed==0 && cancelled==0`. **(4)** Console/ETA work committed as `d995624` (line-by-line console + the 2 PM-2 ETA fixes + 2 tests). **(5) Verification "very quick" = by design** (no change): auto-verify is `verifyFast` (Tier 0+1, ffprobe packet-count/duration/codec via moov atom, seconds) and **auto-escalates** to the Tier-2 byte-exact hash on any anomaly + on-demand via the row magnifier. **(6) Deep-verify label (UNCOMMITTED):** transient `ConversionJob.isDeepVerifying` (out of CodingKeys) → row shows **"Verifying (byte-exact)…"** + detail line + seal tooltip during the Tier-2 pass; build clean+signed, `_run` refreshed, pending user click-through of "Thorough verify" then commit. `main == origin/main` (d995624, 381ff89 pushed); 3 deep-verify files uncommitted. |
| [2026-06-16 (PM-2)](2026-06-16.md) | **3 real-footage issues: console selection · queue ETA · join failures** | **All on `main` (committed); 349 tests/1 skip/0 fail (+6).** Live testing on a real 14-job 4K card surfaced three things. **(1) Console multi-line selection + Copy All** — the console rendered each log line as a separate `Text`, and macOS `.textSelection(.enabled)` can't span sibling `Text` views, so only one line was selectable. Rewrote the body as a **single `Text` from a tinted `AttributedString`** (per-line cmd/info/ok/bad colour preserved via coloured ranges) → drag-select across all lines, ⌘A select-all, ⌘C copy-selection all native; added a **Copy All** button beside Clear (`QueuePanel.swift`). **(2) Whole-queue "time left" was way off** — `SpeedTracker` estimated `contentDuration ÷ speedMultiplier`, wrong for the I/O-bound `-c copy` join (time scales with **bytes**, not playback length), compounded by a 15× no-history default and an average-of-*ratios* bug. Rewrote `remainingQueueSeconds` to **bytes ÷ throughput**: throughput measured **live off the active job** (`activeBytes × progress / elapsed`), fallback pooled `Σbytes/Σseconds` history → conservative ~120 MiB/s default; each pending job contributes its **full source bytes**, so larger splits weigh proportionally more (jobs are sequential → total = active-remaining + Σ pending). Added `SpeedTracker.throughputBytesPerSec` + `defaultThroughputBytesPerSec`; left the duration-based `estimateConversion`/`currentJobEstimate` per-row fallback untouched. **(3) Two jobs failed (`0104` faststart re-open I/O error → truncated 7.44 GB partial; `0108` ffprobe exit 1)** — diagnosed as **transient external-USB I/O under sustained load, NOT app bugs**: volume healthy (APFS, 293 GB free), the "failed" source `dd`-reads end-to-end at 462 MB/s + ffprobes clean *now*, both were jobs #13–14 of a batch hammering one external drive with simultaneous source-reads + output-writes + faststart-rewrites. **Hardening (3 user-approved):** (a) **delete partial on failure** — removes only the file *we* wrote this attempt, never a pre-existing good file (`destinationTouched`); (b) **stage join on internal temp → `moveIntoPlace` to destination** (off-main `Task.detached`; external drive does only sequential reads + 1 write; auto-skips when temp==dest volume via `DiskSpace.sameVolume`); (c) **auto-retry once** (`maxAttempts=2`) gated by `isRetriableJoinError` — retries transient `conversionFailed`/`probeFailed`/unknown, never deterministic `cancelled`/`ffmpegNotFound`/`invalidInput`/`incompatible`/`noVideoStream`/`malformedProbeOutput`. **Bug understood, not silenced:** a flaky weighting test (480 vs 240) was **test cross-contamination** — two managers sharing one `tmpDir` → 2nd loaded the 1st's persisted `queue.json`; production was correct (fixed via per-manager storage dir). **+6 tests.** DMG now lags `main`; only Sparkle Wave 4 gates 1.0-public. *Owed:* real-footage eyeball of the staged-move multi-GB path. |
| [2026-06-16](2026-06-16.md) | **PUBLISHED: public repo + PolyForm license + DMG re-cut** (+ doc reconcile · 4 UI changes · nil-date sort fix) | **AM doc reconcile**, then four user-driven UI changes, all shipped to `main` + pushed + eyeballed. **(1)** File › Choose Folder… split into **Choose Source Folder…** (⌘O) + **Choose Destination Folder…** (`24d14b2`). **(2) Appearance bug fix** (`feb3c43`): Dark → Match System stayed dark — `.preferredColorScheme(nil)` doesn't clear a forced `NSWindow.appearance` on macOS; drive `NSApplication.shared.appearance` (where `nil` reverts) via `.onChange(initial:true)`. → **Cookbook #113** in the Directions repo (`461ce75`) + Vestige. **(3) App icon** (`945ff4d`): new light variant + **runtime light/dark Dock switch** in the Appearance menu (theme radios · Divider · "App Icon" radios). Verified macOS **can't** vary the *bundle* icon by appearance (actool drops dark renditions as "unassigned children"; built `Assets.car` carries only `appearance=(none)`) → `AppIconController` sets `NSApp.applicationIconImage` from `IconPreference`, `.auto` tracks `effectiveAppearance` via KVO; bundle icon stays dark; SVG masters archived to `02_Design/app-icon/`. **Version unchanged 1.0.1/101; 100% feature-complete; only Sparkle Wave 4 gates 1.0-public; DMG lags `main`** (now also owes these). No engine/test change (341/1 skip/0 fail). **— AM portion below —** `/status` found `main` **2 commits ahead of the last log** (both dated 2026-06-14, landed after that day's log) and PROJECT_STATE still describing the old split FeedbackKit + Donate wiring. **Git itself was clean** — `main == origin/main`, 0/0, tree clean → nothing to merge/pull/push; drift was **purely documentary**. Logged it: **`8d8ccad`** replaced the hand-assembled menu wiring with a single **`CitizenshipCommands(citizenship)`** from **AppCitizenshipKit** (`import FeedbackKit` → `import AppCitizenshipKit`; local `DonateCommands` struct collapsed in; `project.yml` drops the direct FeedbackKit dep — now transitive via ACK — and adds `AppCitizenshipKit from 0.1.1`; adds a **link-rich About panel** replacing the stock one; "Donate"/"Support" reworded **"Leave a Tip"** in `help-donate.md` + manifest). **`9c216ea`** fixed stale "Support Conjoyn" comments and bumped **ACK 0.1.1 → 0.1.2** (comment-only). ACK was **generalized from Conjoyn's own #102/#104 patterns then adopted back** — packaging change, not behavior (FeedbackKit still does the work, re-exported; server still gates on `ALLOWED_APPS`). **Version unchanged `1.0.1`/101; still 100% feature-complete; only Sparkle Wave 4 gates 1.0-public; DMG still lags `main`** (now also owes this change). No engine/test change (341/1 skip/0 fail). PROJECT_STATE refreshed to match. **— PM: SHIPPED THE REPO —** (1) **nil-date sort fix** (`a01f138`): undated recordings now pin to the **bottom in both directions** (Finder "—") via a new pure generic `ConversionViewModel.ordered(_:field:by:ascending:)`; `orders()` stays pure ascending-only; **343/1 skip/0 fail** (+2). Verified the no-SRT/copied-MP4s path still joins (SRT optional everywhere; date resolves from the embedded `creation_time` atom, which survives a copy). (2) **Repo public** — `Xpycode/Conjoyn` flipped private→public after a clean pre-public secrets scan (no secrets in tree/history; only Sparkle *public* key; `99-AUTH/` outside repo; FFmpeg LGPL-clean). Added `README.md` + `LICENSE.md` = **PolyForm Noncommercial 1.0.0** (source-available, **no commercial/paid-app use**; licensor Luces Umbrarum) (`1ba9c93`); decision logged to `decisions.md`. (3) **DMG re-cut to 1.0.1/101** — `make-dmg.sh` full run (M1 Max, also a release Mac): app + DMG notary **Accepted**, double-stapled, Gatekeeper-accepted, verified version inside; `04_Exports/Conjoyn.dmg` 28 MB, no longer lags `main`. Logged **i18n** to backlog. **Within this repo the app is done + published-as-source; the only remaining gate to a public download is Sparkle Wave 4 (website, `3-Websites/App-Websites` repo).** |
| [2026-06-14](2026-06-14.md) | **Status check + DMG re-cut + FeedbackKit scaffold → wire-up → FIXED (0.1.0 was broken)** | **Late eve — FeedbackKit didn't actually work; fixed 3 layered bugs, proven end-to-end:** the eve wire-up was structurally correct but **0.1.0 was non-functional**. (1) **0.1.1 sheet never presented** — `.sheet` on a hidden `.background` anchor of the *menu button* (outside any window) → present imperatively via self-managed `FeedbackPresenter` (`NSHostingController`+`NSWindow`, sheet on key window; `onClose` injected). (2) **0.1.2 Send looked dead** — consent checkbox required but its reason buried → highlight consent row + hint in accent when it's the sole blocker (`consentIsOnlyBlocker`). (3) **0.1.3 server rejected with a log attached** — client validated raw `body` but SENT `composedBody` (desc + `recentTail(80)` ≈ 5235 > 5000 cap) → `composedBody` self-caps, trimming oldest log lines. Conjoyn dep 0.1.0→0.1.3, **app `1.0`/100 → `1.0.1`/101** (`b5daa51`/`c09331d`/`8c474f1`/`18fa704`); 341 app + 10 pkg tests. **Real submit (bug "fttttj") landed on the public board** (`apps.lucesumbrarum.com/feedback.html`, unified list w/ type badges — no separate "bugs section"; email→private log). **DMG now lags `main`** (100 vs 101). Rules → memory: presentation modifiers no-op on a `Commands` button; validate the artifact you send; "wired" ≠ "works". PROJECT_STATE `82cf9eb`. <br>**Eve — FeedbackKit wired end-to-end (3 repos, live-verified):** published `Xpycode/FeedbackKit` (**private**) + tag `0.1.0` → `4fddb9e`; wired into Conjoyn (`eede8ff`, pushed) — `project.yml` SPM dep (resolved from the private repo via `gh`), **Help › Send Feedback…**, new write→read `DiagnosticLogger.recentTail()` for the log-attach toggle, `@Sendable`→`@MainActor` bridged via `assumeIsolated`; **341 tests/1 skip/0 fail** (+4); deployed App-Websites (`e1ff415`) allow-listing **conjoyn + manifest + tachograph** across `feedback-submit.php`/`feedback.html`/`feedback.js` to Strato (prod deploy needed explicit auth — classifier blocked the first try); **live gate verified for all three via a consent-omitted probe** (passes app check, creates nothing). Optional owed: a full real submit eyeball. Memory `feedbackkit-in-app-feedback` → DONE. <br>**AM:** Ran `/status` (100% feature-complete, 337 tests/1 skip/0 fail, 1.0 gated only on **Sparkle Wave 4**). "Open app" opened the **stale installed** build 100; user caught it → `xcodegen` → clean ad-hoc Debug build → launched the fresh `main` build. Saved memory `open-app-means-fresh-build`. **PM:** Clarified **Wave 4 (website) lives in the `3-Websites/App-Websites` repo**, not Conjoyn (memory `wave4-lives-in-websites-repo`) → only the DMG re-cut was owed here. **Re-cut `04_Exports/Conjoyn.dmg` from current `main`** (1.0/100, notary **Accepted**, stapled, `source=Notarized Developer ID`, `help-roadmap.md` bundled = confirms HEAD). Fixed **three fresh-release-Mac snags:** `brew install create-dmg`; new `setup-notary-profile.sh` helper (recreate the per-Mac notary keychain profile from `99-AUTH/`); **`notarize.sh` archive now pins Developer ID manual signing** (release-only Mac has no *Mac Development* cert). `create-dmg`'s Finder AppleScript only works in a **GUI session** → ran the DMG step interactively with `SKIP_APP=1`. Two script fixes committed + pushed (`22373d4`); memory `dmg-recut-on-fresh-release-mac`. No engine/test change. **Also started a reusable in-app feedback feature:** new **FeedbackKit** SPM package (standalone repo at `zPackages/FeedbackKit`, local commit `4fddb9e`) — drop-in "Send Feedback…" sheet that POSTs to the existing multi-app `feedback-submit.php` (cookbook #49). Config-injected (no host-app `Theme` coupling), tested core (form-encode + redirect-disabled-302-success + validation), **9 tests green**. User picks: POST-to-PHP · shared apps-portal endpoint · standalone-repo SPM reuse. **Next session = full wire-up** (publish repo + tag, add SPM dep + Help-menu item to Conjoyn with `DiagnosticLogger` log-tail, add `conjoyn` to `ALLOWED_APPS`, live test). Memory `feedbackkit-in-app-feedback`. |
| [2026-06-13 (k)](2026-06-13-help-roadmap.md) | **Roadmap help topic** (in-app Help, content-only) | **Shipped to `main` + pushed (`2655aaf`, merged `--no-ff` via `feature/help-roadmap`).** Added a user-facing **Roadmap** page as the **last item under Reference** in the Help window (user's placement pick over a new bottom group). Audited Help first — it's fully shipped (vendored `HelpMenu` SPM pkg, 9 topics/4 groups, wired to the `?` toolbar button + Help menu); old PROJECT_STATE backlog item (7) was stale. **Drafted only honest futures:** **watch-folder ingest** (spec v1 scope but never shipped — only a stale comment at `RecordGroup.swift:10`) and **more camera families** (GoPro/Osmo; engine already camera-agnostic), plus an "exploring" metadata-grouping mode and a link-less "Have a request?" close. **Auto-update deliberately omitted** — it ships *in* 1.0, so a reader already has it; listing it would read as "coming soon" for a live feature. **Content-only:** new `help-roadmap.md` + one manifest row, **no Swift / no build config** (recursive `Conjoyn/` source ref auto-bundles the `.md`; `.xcodeproj` gitignored). `xcodegen` regen → ad-hoc Debug build **SUCCEEDED**, `.md` confirmed in `Contents/Resources/`, app launched. **Owed:** eyeball the live page; wire the feedback link once the site exists (pairs with Wave 4). DMG now also lags `main` by this topic. Only **Sparkle Wave 4** gates 1.0-public. |
| [2026-06-13 (j)](2026-06-13-diagnostic-logging.md) | **Persistent diagnostic logging** — closes the last `/minimums` gap | Ran `/minimums` (verified against code, not just the log): everything covered **except** persistent diagnostic logging — `QueueManager.log()` wrote only to an in-memory console (`consoleLines`), so a bug filed after a quit/relaunch had nothing on disk. One deliberate omission confirmed kept: **no Preferences window (⌘,)**. **Shipped `DiagnosticLogger`** (`5a11fc6`): file-backed log at `~/Library/Application Support/Conjoyn/diagnostic.log` — `@MainActor` singleton, **injectable storage dir** (matches `SpeedTracker`), ISO-8601 stamps, per-session banner with bundle version, append via `FileHandle.seekToEnd`, **single-generation rotation → `diagnostic.log.1` at 1 MB**; every `FileManager`/`FileHandle` call `try?` so logging can't crash the app. **One line** in `QueueManager.log()` mirrors all ~56 existing call sites for free (the high-frequency `speed=`/progress stream goes through `activeMetrics`, not `log()`, so writes stay cheap). `DiagnosticLoggerTests` (7: banner/version, append, ordering, injected dir, rotation, `.1` replacement, no-rotate-below-threshold; rotation tested by pre-seeding an oversized log). **337 tests, 1 skip, 0 fail.** Debug build ad-hoc-signed (release-only Mac). **Owed:** one live eyeball that the real log file materializes on a join. **DMG lags `main`** (now +logging). Only **Sparkle Wave 4** gates 1.0-public. |
| [2026-06-13 (i)](2026-06-13-menu-single-window.md) | **Menu polish + single-window mode** (UI only, no engine change) | **3 changes shipped to `main` + pushed (`dc88a42`).** **(1) File › Choose Folder…** (⌘O) — new `FileCommands` reusing `chooseSourceFolder()` (same path as the toolbar Scan). **(2) Edit menu trimmed below Select All** — Dictation+Emoji via the documented `UserDefaults` keys (`NSDisabledDictationMenuItem`/`NSDisabledCharacterPaletteMenuItem`), **Writing Tools** (no opt-out key) via a new `EditMenuTrimmer` `NSMenuDelegate` deleting items after `selectAll:` on every `menuNeedsUpdate`. **(3) `WindowGroup` → `Window("Conjoyn", id: "main")`** — single-instance scene drops "New Window" (⌘N) + the tab bar for free (app shares one view model/queue → multi-window only mirrored state; verified zero other multi-window deps). Spec written first: `specs/single-window-mode.md`. **Side-quests:** recovered from a stale-artifact scare (the notarized `04_Exports` zip predates the light-theme merge → Appearance looked "missing"; confirmed merge is in HEAD); established the **ad-hoc-signed Debug** build path on this release-only Mac (no *Mac Development* cert). User eyeballed all three builds. Engine/tests untouched (330). **Owed:** re-cut the DMG (lags `main`); eyeball single-window reopen-after-close. Only Sparkle Wave 4 still gates 1.0-public. |
| [2026-06-13 (h)](2026-06-13-git-bootstrap.md) | **Git bootstrap discipline + Directions master reconcile** (no app code — pure infra/meta) | **Properly gitted two `.git`-less folders + hardened the multi-Mac discipline.** This Mac had **no `.git`** (Syncthing `.stignore`-excludes it; history lives only on `origin`, HTTPS-via-`gh`, no SSH). Bootstrapped Conjoyn safely: `init → fetch → **reset --mixed origin/main** → git status` — **0 tracked drift** (every Syncthing'd file byte-identical to `origin/main` `5a69c6f`) → clean adopt, **no `reset --hard`**; gitignored `.claude/settings.local.json` (`1f6c8da`). **Generalized** it: new global **`git-bootstrap` skill**, wired the global CLAUDE.md Multi-Mac pre-flight to auto-detect the no-`.git` case, + a Conjoyn memory. **Corrected the unsafe `reset --hard` advice** in Conjoyn `PROJECT_STATE` (`2dd00db`) **and** the Directions master `37_multi-mac-discipline.md` (new **Rule 1b** + a Rule 1 verify-first guard). **The discipline then caught a real divergence:** bootstrapping the master to push, the mixed-reset+`git status` check found it **5 commits stale** (missing Rule 5, `/worktree`, hooks, cookbook #91) — a blind push would have **deleted Rule 5 + 4 files**; re-applied the edit onto origin's current version instead (`3d10e3c`). **Full guided 3-way reconcile** of the master's local-only work: backup-branch snapshot → adopt origin → re-applied **cookbook #91-97 renumbered → contiguous #92-98** (origin had claimed #91; +1 propagated through filenames, H1 headings, intra-group `#9N` cross-refs, and the index) + **#00 §0.2** grafted (additive `UIDesignRequiresCompatibility` in a checked-in `.xcodeproj`); kept origin's newer `PROJECT_STATE`/`status.md`/`session-start.sh` (`5255d19`, pushed `Xpycode/LLM-Directions`). **Conjoyn ripple:** live cookbook pointers `#94→#95`, `#91/#92→#92/#93` (`bd47d74`); session logs left as history. Deleted the served backup branch; **both repos clean + in sync**. **No change to Conjoyn status** — 100% feature-complete, 1.0 still gated only on Sparkle Wave 4. |
| [2026-06-13 (g)](2026-06-13-quicklook-thumbnails.md) | **QL thumbnail fix — QuickLook-first row thumbnails with FFmpeg fallback** (backlog 3) | **Shipped to `main` + pushed (`ab6d140`).** Row thumbnails now come from `QLThumbnailGenerator` instead of an FFmpeg subprocess per clip — QuickLook decodes **out-of-process** (system Thumbnails agent) and is **system-cached** (file+mtime+size), so the decode load leaves the app (eases the documented post-scan thumbnail/SRT I/O contention) and a **re-scan returns instantly**. **Hybrid:** the FFmpeg first-frame path is **kept as a fallback** for any file QuickLook can't read, so the throttle-semaphore / kill-poll / `ContinuationGuard` machinery is *demoted* to the rare-miss path, not deleted (no blank tiles ever). Also **dropped the unused last-frame extraction** (the row only ever showed `first ?? last` → `last` was a second subprocess per clip for a never-shown frame; now always nil, field kept so call sites don't churn). **`representationTypes: .thumbnail` only** — `.icon`/`.all` can "succeed" with the generic movie-file icon, which would suppress the fallback and show the same glyph on every row. `import QuickLookThumbnailing` **auto-links** (no `project.yml`/xcodegen change; SourceKit's "Cannot find type" was a false index-not-loaded warning, real build clean). **330 pass / 1 skip / 0 fail** — integration test flipped to the new contract (`first` non-nil via QL-or-fallback, `last` nil); the "missing source → empty but cached" test unchanged. **User-confirmed "definitely faster"** on a real card. Pattern → **cookbook #94** (`94-macos-quicklook-thumbnail-hybrid.md`). **Push gotcha:** `main` had no upstream → first `git push` silently no-op'd (tell: `## main`, not `## main...origin/main`) → fixed with `push -u origin main`. Only Sparkle Wave 4 (website publish) still gates 1.0-public. |
| [2026-06-13 (f)](2026-06-13-light-theme.md) | **Light theme eyeball + merge to `main` + branch cleanup** | Closed the two owed light-theme items and shipped. **Clean build cycle** → launched, user switched **Appearance › Light** and confirmed live on the 74-clip `2CULL` card. **Eyeball:** palette + tint accents sharp; tint concern cleared *transitively* — the `SPLIT · 4` badge is the same construction as the integrity chips (`acc1` 12% fill + 28% stroke) and reads crisply. **Honest gap:** the green/red verification seal only renders post-join (`0 of 14 joined`) and chips only on flagged clips (all clean here) → neither on screen; user chose **trust the seal** (full-opacity, high contrast by construction) over running a join. **Tests 330/1 skip/0 fail.** **Merged `feature/light-theme` → `main` `--no-ff` (`ebd260d`)** — branch was **stacked on `feature/sparkle-update`**, so this one merge also brought **all of Sparkle Waves 0–3 onto `main`** (user chose "merge both"). Pushed (`4244ab2..ebd260d` + doc sync `06a329b`); origin/main in sync. **Branch cleanup:** deleted the 2 merged branches + 3 stale merged origin refs (`manual-tc-override`, `output-folder-clarity`, `rename-tc-disclosure`); **left** `claude/solo-dev-marketing-strategies-c9smu6` (5 unmerged non-code commits — deleting would orphan the only copy). Repo now = `main` + that one parked exploration. **Only Sparkle Wave 4 (website publish) gates 1.0-public.** |
| [2026-06-13 (light theme)](2026-06-13-light-theme.md) | **Light theme — Auto/Light/Dark appearance toggle** | Added a user-selectable appearance via a top-level **Appearance** menu (Match System / Light / Dark), default **Dark** so the out-of-box look is unchanged. **Key finding:** `.preferredColorScheme(.dark)` wasn't what made the app dark — `Theme.swift` held fixed-hex colors, so a real light mode needed a second palette + adaptive tokens. Made all 13 `Theme` tokens adaptive via dynamic `NSColor` (resolves against the window's live `NSAppearance`) — one chokepoint, no call-site churn (136 usages, zero stray `NSColor`/`.gray` in views). New "soft neutral gray" light palette (accents unchanged); two helpers `Theme.raised()`/`Theme.recessed()` replaced 18 inline `.white/.black.opacity()` chrome colors + 2 stray hex across `DesignControls`/`RenamePopover`/`RecordingsList`. `AppearancePreference` enum + `@AppStorage` + `AppearanceCommands`. Build clean (Swift 6); user confirmed light mode "looks really good." **Diverges from the App Shell Standard** (dark-only) — flagged. Isolated on **`feature/light-theme`** (rebased onto sparkle-update tip). Owed: light-mode accent eyeball (seal/chip on white). |
| [2026-06-13 (cont.)](2026-06-13.md) | **Sparkle key backup #2 — custody secured** | Closed the last R1-risk owed item before public release. Located backup #1 at `/Users/sim/ProgrammingProjects/99-AUTH/` (one level **above** the repo → genuinely out-of-repo, in no git tree; explains the earlier "not ignored" reading). Confirmed **this is the M4 Pro** (key-custody Mac), `conjoyn` account in keychain. **Verified backup #1 without exposing the secret:** signed fixed bytes with the keychain key (`--account conjoyn`) and the file (`--ed-key-file`) via Sparkle's own `sign_update` → **bit-identical Ed25519 signatures** (deterministic) = byte-for-byte the same key. **Backup #2 = password manager** (off the Syncthing mesh — backup #1's `99-AUTH/` is Syncthing-replicated, so keychain+file share a failure domain): seed copied via `pbcopy` (never printed; length-44 confirmed), pasted into a secure note with recovery metadata (public key `Ks14npeWNt9Rd8…`, `account=conjoyn`, both locations, restore steps), clipboard then **cleared** (0 chars). Key custody now = **3 verified-identical copies** (keychain + 99-AUTH file + password manager). Recovery check of record: a restored seed is *the* key iff it derives the shipped `SUPublicEDKey`. **Only Wave 4 (website publish) remains.** Memory + PROJECT_STATE updated. |
| [2026-06-13 (cont.)](2026-06-13.md) | **Apple notary round-trip + Sparkle Wave 3 (release pipeline + self-update dry run)** | **Notary Accepted** — `notarize.sh` archive→export, **8/8 nested Mach-Os Developer ID + hardened runtime**, Apple `status: Accepted` (id `3c43b140…`), stapled, `spctl` = `source=Notarized Developer ID`; closes owed item (b) — the adhoc-Sparkle fix is now Apple-confirmed, not just locally audited (`324e461`). **DMG re-cut** Sparkle-enabled via `make-dmg.sh SKIP_APP=1` (DMG notary Accepted, stapled). **Wave 3.1:** wrote `make-appcast.sh` (DMG enclosure, `--account conjoyn`, `--download-url-prefix`; stages `Conjoyn-<short>.dmg`, runs `generate_appcast`, hard-verifies xmllint/`sparkle:version==build`/exact `length`/`edSignature`/URL) → signed `appcast.xml` (auto `hardwareRequirements arm64`) (`8f201e6`). **Wave 3.2 PROVEN:** built throwaway notarized **1.0.1/101** DMG + local-feed appcast, served over **mkcert HTTPS** :8443 (CA already System-trusted), `defaults write …SUFeedURL`, launched build-100 → **Check for Updates… → 1.0.1 → downloaded → EdDSA-verified → installed → relaunched as 101** (user-confirmed via screenshots); server log shows appcast + DMG GETs; swapped app stayed notarized + stapled. **Restored all test state** (project.yml→100, /Applications→100, real DMG→100, SUFeedURL deleted, server + tmp wiped) (`1e7b5f4`, pushed). Gotcha: a fresh `generate_appcast` dir re-prompts for the keychain key → Always Allow; a detached bg process hangs silently on it. **Sparkle complete through W3; only Wave 4 (website publish) + key backup #2 remain.** |
| [2026-06-13](2026-06-13.md) | **Sparkle Wave 0 (EdDSA key) + Wave 2 (local verify), on the M4 Pro** | Git reconcile found **nothing from the M1 Max** (both sync channels agreed — Syncthing excludes `.git`, origin unchanged at `30b5e8e`, `SUPublicEDKey` still placeholder → Wave 0 never done there). M1 Max out of order → continued on the M4 Pro. **Wave 0 (`a53d080`):** no Conjoyn key existed yet, so fresh `generate_keys --account conjoyn` here — **public key `Ks14npeWNt9Rd8QawQiBYQuzFq08vPe2hXgu1s5zVOE=`** in `Info.plist` (round-trips via `-p`); private key in M4 Pro keychain + backup #1 `99-AUTH/conjoyn-sparkle-private.key` (chmod 600); **backup #2 owed**. The `--account` flag (plan's open question) confirmed supported. **Key custody moved to the M4 Pro**; keychain audit confirms it holds the full Developer ID Application identity → complete release Mac. **Wave 2:** 2.1 build + **330 pass/1 skip/0 fail**; 2.2 "Check for Updates…" smoke ✅ (menu enabled, Sparkle sheet on click); **2.3 risk gate FIRED** — plain `xcodebuild build` left all 4 nested Sparkle Mach-Os **adhoc** (`--deep --strict` passes anyway = notary-rejection trap), **fixed (`aeb517b`) by switching `notarize.sh` to Archive→Export (`method=developer-id`)** + an `assert_devid_runtime` loop over all 8 nested binaries; re-audit shows **all 8 Developer ID + `flags=0x10000(runtime)`**; `make-dmg.sh` repointed to `export/Conjoyn.app`. 2.4 version `1.0/100`. **Apple notary round-trip not yet run** (local audit passes → safe deliberate next step). Next: run `notarize.sh`, then Wave 3 (`make-appcast.sh` + 100→101 dry run). |
| [2026-06-12 (f)](2026-06-12.md) | **Executed Sparkle Wave 1 (local integration)** | `/execute` of Wave 1 from `docs/plans/sparkle-auto-update.md` on `feature/sparkle-update` (`11958e6`). Pure code: Sparkle 2.9.3 SPM dep + target dep; version baseline **0.1.0/1 → 1.0/build 100**; 3 `SU*` keys in the **base Info.plist** (cookbook #89 trap dodged — confirmed merged); `UpdaterController.swift` ported verbatim from P2toMXF; `UpdaterCommands` (`Commands` struct, not closure) adds "Check for Updates…" after `.appInfo`. **BUILD SUCCEEDED**; `Sparkle.framework` auto-embedded with all 4 nested Mach-Os (Wave 2's notary-audit targets); built plist carries build 100 / 1.0 + all SU keys. `SUPublicEDKey` left as `__FILL_FROM_WAVE_0__`. **Key custody deferred to the M1 Max** — `99-AUTH/` has Penumbra's Sparkle key but **none for Conjoyn**, and keys are per-app, so Wave 0 must create Conjoyn's own key where signing custody lives (memory `signing-mac-and-sparkle-keys`). Next: Wave 0 on the M1 Max → Wave 2. |
| [2026-06-12 (e)](2026-06-12.md) | **Decided + planned Sparkle auto-update (no code)** | Closed the scope gap: **ship Conjoyn ONLY with auto-update** (first public DMG = first Sparkle build). Verified the (d) research against P2toMXF (verbatim controller/menu), Penumbra's runbook, and **live Sparkle 2.9.3** docs (3 agents + web/MCP). User chose **DMG-only enclosure · automatic+menu checks · debut 1.0/build 100**. Wrote `docs/plans/sparkle-auto-update.md` (5 waves: integrate → keys → verify → release pipeline → publish), grounded in Conjoyn's real files. Verification catches: **`--account` flag unverified** (resolve at exec; rely on `-x` export + 2× backup either way); shipped binary is really **0.1.0/build 1** ("v1.0.1" was narrative) → clean reset to 1.0/build 100; real signing risk = the **4 new nested Sparkle Mach-Os** (`notarize.sh` audit + Archive→Export fallback added). Non-sandbox gift holds: **zero new entitlement/build setting**. Logged to `decisions.md`, synced PROJECT_STATE. Next: execute Wave 1. |
| [2026-06-12 (d)](2026-06-12.md) | **Sparkle auto-update research (pre-planning, no code)** | Reconnaissance for next session's planning — fanned out **3 read-only agents** over every shipped sibling's Sparkle setup (Penumbra, CropBatch, P2toMXF, cookbook #16, AppUpdater, DiskVerdict/QuickMotion/AspectRatioUnifier/syncthingStatus). Consolidated into **`docs/sparkle-research.md`**. **Headline: Conjoyn is the *simplest* Sparkle case in the family** because it's non-sandboxed — **no new entitlement, no new build setting** (the existing FFmpeg `cs.disable-library-validation`/`allow-unsigned-executable-memory`/`allow-jit` are exactly what Sparkle's XPC needs; drop `network.client`/`SUEnableInstallerLauncherService`/`-spks`/`-spki` — all sandbox-only). **P2toMXF code is copy-verbatim** (`UpdaterController.swift` + `FileMenuCommands`/`@StateObject` menu) but it never finished (placeholder `SUPublicEDKey`) — take the *process* from Penumbra (which wrote `sparkle-release-runbook.md`). **xcodegen wrinkle:** declare SPM dep in `project.yml`, put `SU*` keys in the **base Info.plist** (`INFOPLIST_KEY_*` silently drops them — cookbook #89 trap class). `AppUpdater/` is irrelevant (reads *other* apps' feeds). **Landmines catalogued:** EdDSA key-loss = users never update again (generate fresh, back up 2×, `--account conjoyn`); never `codesign --deep`; notarization inspects Sparkle's nested binaries; `sparkle:version` = monotonic int; enclosure `length` = exact bytes/no recompression; first release can't self-test (999→1000 staging). Self-host like Penumbra; enclosure → raw DMG path, not the counted/302 PHP endpoint. **Open for planning:** DMG vs ZIP enclosure, feed URL shape, version pin, script wiring. No code, no decisions. |
| [2026-06-12 (c)](2026-06-12.md) | **Ship v1.0.1 (commit + merge + DMG re-cut); surfaced the auto-update gap** | Reconciled git first — local `main` already == `origin/main` at `cd28fe8` (the DMG-wrapper merge was already in; the (b) "Next" to merge it was stale). Committed the uncommitted sortable-columns tree on `feature/sortable-columns` (`00a74b2`: VM + RecordingsList + new `RecordingSortTests.swift`), **re-ran the full suite at merge time → 330 pass / 1 skip / 0 fail**, merged `--no-ff` (`b32ffe5`), pushed `main` (`cd28fe8..1d5795d`), deleted the branch. **Re-cut the DMG as v1.0.1** via `make-dmg.sh` (fresh app build, not `SKIP_APP=1`, so it carries the sortable-columns binary): app notarization **Accepted** → DMG notarization **Accepted** → stapled → Gatekeeper `accepted · source=Notarized Developer ID`; `04_Exports/Conjoyn.dmg` (~26 MB) installs offline. **Surfaced a scope gap:** Conjoyn has **no auto-update mechanism** — a full code search found zero Sparkle/appcast integration; never built, never decided against, never deferred (only the unchecked `[ ] Auto-update` line in the generic `33_app-minimums.md`). Root cause: v1 scope was engine+GUI+watch-folder+SRT with distribution = "direct + notarized", and notarized got conflated with "distribution done". Favorable timing — no installed base yet (website still backlog), so adding Sparkle now is greenfield. **Open decision:** add Sparkle before the first public download link, or defer. |
| [2026-06-12 (b)](2026-06-12.md) | **Sortable recordings columns (backlog 12, → v1.0.1)** | First post-1.0 feature. Finder-style clickable column headers (Name·Date·Duration·Size) on the discovered-recordings list. New `SortKey` + `setSort` (re-click flips, switch adopts column default) on `ConversionViewModel`; sorting folded into the `filteredGroups` chokepoint via decorate-sort-undecorate (groups/selection/rename cache untouched); pure static `orders(_:before:by:)` over a `SortField` with a stable `groupIndex` tie-break + `localizedStandard` name order. New `ColumnHeaderBar`/`SortLabel` + shared `CJRowMetrics` so header columns pixel-align over rows. **+11 tests → 330 pass.** Two live-eyeball fixes: header **ballooned** (greedy `Color.clear` spacers → padding-based inset) and **"DURATION" wrapped** (col 66→80 + `lineLimit(1)`). **Resort-beachball fix:** the date sort was resolving `.SRT` files off the SD card **synchronously in `filteredGroups`** (main thread); now the sort reads only the resolved cache (embedded-date fallback) and `prewarmStartDates()` resolves all dates off-thread (`Task.detached .utility`, `Sendable`-only capture) after scan, bumping `@Published startDateRevision` to re-sort once warm. User: "works fantastic." **Merged to `main`.** nil-date policy = `.distantPast` (Finder "always-last" left as a `TODO`). |
| [2026-06-12](2026-06-12.md) | **DMG wrapper — the last release task** | **Conjoyn is now shippable.** Reconciled git first (no `.git` on this Mac — Syncthing excludes it; wired `origin`, 3 files MD5-identical, `reset --hard origin/main` `1c47bfe`). Recreated the missing per-Mac `conjoyn-notary` keychain profile from `99-AUTH/` secrets (`.p8` + issuer from `IssuerID.rtf`), validated. Wrote `01_Project/scripts/make-dmg.sh` (mirrors `notarize.sh`; **delegates** app build+notarize+staple to it, then `create-dmg` styled window + `/Applications` drop-link + volume icon → `codesign --timestamp` → notarize + staple the DMG; `SKIP_APP=1`, one `hdiutil busy` retry). Installed `create-dmg` 1.2.3. **Both notary round-trips Accepted**; verified end-to-end — DMG `source=Notarized Developer ID`, mounted → app inside stapled + Gatekeeper-`exec`-accepted, drop-link present. **`04_Exports/Conjoyn.dmg` (27 MB) installs offline, no Gatekeeper prompt.** On `feature/dmg-wrapper`. (`spctl -t open` needs `--context context:primary-signature` for DMGs — quirk, not a failure.) |
| [2026-06-11h](2026-06-11h.md) | **Help screenshots added to all topics** | 5 images captured from the live app, placed in `Help/` alongside markdown, xcodegen regenerated. Placement refined after first check (removed redundant `main-loaded.png` from 2 topics). Two UX observations: thumbnail extraction eager on 74 items (fix: QLThumbnailGenerator); no way to select splits while staying in All view (rescan workaround). DMG is still the last ship task. |
| [2026-06-11g](2026-06-11g.md) | **Help window + filter visibility + fps fix + all eyeballs cleared** | fps now uses r_frame_rate (codec-signaled). Recording filter buttons show/hide rows + auto-select. Help window shipped (vendored HelpMenu package, 9 topics, ⌘? toolbar button). SRT-mismatch chip eyeballed via test fixture. Slow-mo chip confirmed live. 319/319. Only DMG left to ship. |
| [2026-06-11f](2026-06-11f.md) | **Eyeball-fix plan executed + Start button moved to bottom-right** | 6 tasks shipped: "Processing…" label for singles, chip filter `>= .warning` (no false ⚠ Duration chip), mapStatus thorough hash pass → green seal, seal animation stops immediately, queue restore banner, queue row type/duration/size. +2 tests → 317/317. Live-verified: both jobs green seals, no false chips. Start/Stop moved to bottom-right (HIG primary action placement). All pushed. |
| [2026-06-11e](2026-06-11e.md) | **Eyeball test + planning — 4 owed items live-verified, 6 issues found, plan written** | All 4 owed eyeballs passed (TC override ✓, verification seal ✓, ETA/speed ✓; slow-mo chips untestable). Issues found from screenshots: "Joining…" label for singles, ⚠ Duration chip on every join (info-level delta shown as chip — filter bug), mapStatus not promoting thorough hash pass to green, seal animation stop, silent queue restore, queue row loses recording info. **Plan written:** `docs/plans/eyeball-fixes-2026-06-11e.md` — 6 tasks, 2 waves. Decisions: chip filter → `>= .warning`; thorough hash pass → `.verified`. ETA-includes-verification deferred. |
| [2026-06-11d](2026-06-11d-stub.md) | **Backlog (9): manual TC override shipped + UX iteration** | `TimecodeField` (orchetect/swift-timecode v3.1.2); inline pencil button on Applied TC row → popover with auto-advance field + Set/Revert. +10 tests → 315/315. Merged + pushed. |
| [2026-06-11c](2026-06-11c.md) | **Backlog (9): research + plan for per-recording manual TC override** (HH:MM:SS:FF entry field in queue-row disclosure panel) | **Plan written, no code.** Researched 3 reference projects + TimecodeKit library. Decided: use `orchetect/swift-timecode` v3.1.2 (`TimecodeField` SwiftUI-native, Swift 6 compatible, macOS 14+). Override stored as `var timecodeStringOverride: String?` on `ConversionJob` (excluded from `CodingKeys` → session-only). Overrides only the `-timecode` ffmpeg arg, not `creation_time`. 4-commit plan: (1) model+engine, (2) `TimecodeDisclosure.build()` reflects override, (3) TimecodeKit dependency + xcodegen, (4) UI in `TimecodeDisclosurePanel`. ~305→314 tests. Plan embedded in session log, ready to `/execute`. |
| [2026-06-11b](2026-06-11b.md) | **Backlog (6): true source↔target verification of every lossless join** (replacing the dead decode-only check), executed wave-based (`/execute`) against an in-repo plan | **Shipped to `main` + pushed (`bca191c`). 305/305** (was 263; +42). New `SourceTargetVerifier`: **Tier 0** readability gate · **Tier 1** fast container-index compare (exact packet count+bytes, ±1-frame duration, codec-param identity, A/V drift) · **Tier 2** opt-in byte-exact per-stream packet-MD5. Auto fast-verify fires after each join (live source scope), auto-escalating to the hash on any anomaly; a per-row green/orange/red **seal** + non-pass chips + a manual **"Thorough verify (byte-exact)"** button surface it. New `VerificationStatus.warning`; old decode-only `VerificationService` left unwired. **Caught + fixed a pipe-buffer deadlock** (`cf3de85`) that would have hung the queue after every real join — ffprobe's per-packet stdout overflows the ~64 KB pipe buffer; stdout now captured to a temp file (diagnosed by `sample`-ing the stuck process). Real-ffmpeg integration test: byte-identical pass + tampered-source fail. **Owed:** live GUI eyeball of the seal on a real card. |
| [2026-06-11](2026-06-11.md) | **Backlog (5): per-recording metadata-integrity flags on the recordings list** (planned with Explore + Plan agents + a web/HIG check), then live-iterated; plus two UI fixes from the eyeball | **All shipped to `main` + pushed. 263/263** (was 250; +13). **(1) Integrity flags** (`e74f431`, feature `3a7a238`): new pure `RecordingIntegrity` service built lazily per row (`.task`), reusing the engine's `RecordingStartResolver` + `TimecodeDisclosure.detectSlowMotion` (no engine coupling, skips the absent-for-DJI `tmcd` read). The row's date line now shows the **corrected** date + an origin tag ("from filename"/"from SRT cue") instead of parroting a wrong/missing embedded date; a chip strip appears **only** for genuine problems (no date / bad date / SRT↔filename mismatch) + slow-mo, each with a `.help()` tooltip. `CJBadge` gained `isFlagged` (flagged lone clip → orange SINGLE badge, making single-file re-export discoverable). Plan-agent catch: `displayStartDate` returned raw embedded date only; real DJI integrity story is date *provenance*, not source-`tmcd`. Dropped the redundant "date from X" *chip* (it duplicated the inline date-line tag). **(2) Queue progress-bar** (`d96bc51`, `119e2c3`): the "black rectangle" on queued rows was `CJProgressBar`'s empty 35%-black track at 0% fill — now hidden for `.pending` jobs (clear filler keeps the status column aligned), bar shows once preparing/running/done/failed. **(3) Thumbnail placeholder** (`1d6136a`, `abe96eb`): flattened the 3D-ish gradient tile, then removed the `play.fill` glyph (flat tile + ▶ read as a clickable play button) → bare flat dark tile. **Live-confirmed** the SRT path on the SRT-bearing card (all "from SRT cue", clean → no chips). **Diagnosed** the "thumbnails not loading" as a non-bug: `ThumbnailManager` caps extraction at 3 concurrent FFmpeg procs, so large 4K segments just fill in slower. **Owed:** live-eyeball the slow-mo + SRT-mismatch chips (no such clip on the cards seen). |
| [2026-06-10p](2026-06-10p.md) | **Backlog (4): live ETA + speed readout during a join** — planned against the references (Penumbra + `_reference/P2toMXF`), then built | **Shipped to `main` + pushed (merge `2f0cde4`, feature `a9f4de1`). 250/250** (was 245; +5). Display-only; lossless join path untouched. **Research keystone:** it was a *wiring* job — the ETA machinery (`ProgressMetrics.estimatedRemainingSeconds`, `currentJobEstimate`, ffmpeg's live `speed=`) was **ported from P2toMXF and sitting unused**; Penumbra has no ETA. Two scope calls with the user: **per-row + footer total**, and **show the speed multiplier too** (the only part needing plumbing — the live `ProgressMetrics` was being discarded after the slow-speed check). **Impl, no new files:** `QueueManager.activeMetrics` (`@Published`, ffmpeg's live metrics, fed by the `metricsHandler`, cleared at job start/end); testable `remainingQueueSeconds(at:)` = active job's live `elapsed/progress` remaining (historical `currentJobEstimate` fallback < 5%) + `getTotalQueueEstimate()` for pending jobs; shared `formattedCoarseDuration()` (`ConversionEstimate.formattedEstimate` de-duped onto it); `QueueRow` shows `12.5× · ~2:34 left` inline (1 s `TimelineView`, active rows only), `FooterBar` shows the whole-queue `· ~N min left` while processing. Per-row ETA is **history-independent** (robust on a fresh install); only the pending-job total leans on history. **+5 tests** (`formattedCoarseDuration` + the four `remainingQueueSeconds` paths). Merged `--no-ff`, branch deleted, app launched. **Owed:** live eyeball during a real join (can't be driven headlessly) + 2 cosmetic confirmations (140 pt metrics column at the 1220 pt min window; `×` vs plain `x`). |
| [2026-06-10o](2026-06-10o.md) | **Two UI polish items:** (1) backlog 5b — codec · resolution · fps on each recording row; (2) font cleanup (SF Pro for filenames/paths, SF Mono only for timecode) | **Both shipped to `main` + pushed. 245/245** (was 238; +7 formatter tests). **(1)** Pure display add — the scan already reads `VideoStreamParams` per clip for the param guard, so no new I/O. New `CJFormat` helpers (`codec` hevc→HEVC/h264→H.264, `resolution` 3840×2160, `fps` 25/29.97 fps, blank when indeterminate) + `RecordingRow.streamSummary` reading `group.clips.first?.streamInfo?.video` (first segment speaks for the group — the grouping gate refuses mismatched codec/res/fps). Placed between the name VStack (holds the flexible space) and the SINGLE/SPLIT badge, so the title truncates first when cramped. `CJFormatTests` +7. **Live-confirmed** on the `2CULL` card (`HEVC · 3840×2160 · 25 fps`, a 29.97 fps single); also confirmed the wrong-folder empty state ("No video segments found in 2CULL-2025-08"). Merged `7581f96`→`6683656`. **(2)** The "serif-like" font was **SF Mono** (`design: .monospaced`). Switched segment-sublist filenames (`RecordingsList:218`) + queue-row disclosure **Output** path (`QueuePanel:446`) to **SF Pro** (kept `.monospacedDigit()` for column/date alignment); **TC stays SF Mono** (Source/Applied TC, user wants this); console log + Rename token field left mono (terminal/token alignment). Path wells + queue-row title were already SF Pro (not the culprit). Display-only, no test change; user confirmed the look. Merged `d38331e`→`6adb44a`. |
| [2026-06-10n](2026-06-10n.md) | **Fix the min-window Output-bar bug; live-verify output-folder A+B; merge + push** | **DONE — feature shipped to `main` + pushed.** (1) **Window fix** (`3928f47`): at `minWidth: 1000` the Output bar overflowed (truncate/wrap/clip) because `.windowResizability(.contentMinSize)` doesn't derive the bar's floor through the `VSplitView`. Measured the bar's intrinsic width via AppKit font metrics (~1160 pt worst-case: well + 4 switch labels + gear + "Add N to Queue"), set root `minWidth: 1220` + `.fixedSize()`-ed the "Output"/switch labels as a backstop. Drag-verified live (labels intact, no clip); 238/238 tests. (2) **Output-folder A+B live-verified** on a real 60-job queue: per-row "Output" disclosure showed each job's destination (Part A transparency); changing the Output folder fired the **"Apply new output folder to 60 pending jobs?"** popover (Part B); **Keep** lit the orange ⚠ badge + `⚠ → …/DJI_001 (≠ current output)` sub-line on every pending row; **Apply** re-pointed all 60 and cleared the badges reactively. (3) Committed the fix on the branch, merged `feature/output-folder-clarity` `--no-ff` → `main` (`37aca3f`), deleted the branch, **pushed** (`e6a2fb1..37aca3f`). (4) **False-alarm "crash" diagnosed:** the app never died (10-min uptime, zero crash reports) — a file-dialog beachball entering the USB `2CULL-IN/DJI_001` folder (349 items → thumbnail/enumeration storm; compounded by the boot disk at 96 % full), after which the window slipped behind another app. No code defect. |
| [2026-06-10m](2026-06-10m.md) | **Reconcile git after the Mac switch**, then **implement output-folder ↔ queue clarity (A+B)** from the in-repo plan; build + launch; logged a min-window-size bug | **Git reconciled + feature implemented (committed, unmerged).** (1) No `origin` was set on this Mac and `main` was stale at `3e27526`; later-session work showed as "uncommitted" only because `.git` never got the commits (Syncthing excludes `.git`). Wired `origin`→`github.com/Xpycode/Conjoyn`, fetched (`origin/main`=`e6a2fb1`), proved on-disk source byte-identical to origin (only the gitignored generated `.xcodeproj` differed), user-approved the auto-blocked `reset --hard origin/main` → `main`==`origin/main`, regenerated `.xcodeproj` via xcodegen, removed the resolved handoff banner. (`git-remote-reconciliation` memory doesn't exist on this Mac — memory isn't Syncthing'd.) (2) **Feature `2eb0143`** on `feature/output-folder-clarity`, exactly per plan, **no new files**: `QueueManager.directoriesDiffer(_:_:)` + `reassignPendingDestinations(to:)` (+private `uniqueDestination`); VM `showApplyFolderPrompt`/`pendingJobCount`/change-detect in `chooseOutputFolder()`/`applyOutputFolderToPendingJobs()`; `QueuePanel` `ApplyFolderPopover` + per-row ⚠ badge + `≠ current output` sub-line + always-on "Output" row in the TC disclosure. **+9 tests → 238/238** (symlink test rewritten to use a real dir+symlink — macOS only canonicalizes symlinks for existing paths). Debug build SUCCEEDED, launched. **NOT merged** — live A+B GUI verify still owed. (3) **NEW BUG:** at the `minWidth: 1000` floor the Output bar middle row mangles ("utput" clipped, "Add to Queue" cut off, "Timecode…" wraps). 3 reference screenshots saved to `03_Screenshots/min-window-size_2026-06-10m/`. Next: bump root `minWidth` to ≈ the "Choose-button-stops-moving" width, live-verify A+B, then merge. |
| [2026-06-10l](2026-06-10l.md) | **Plan the output-folder ↔ queue clarity (A+B)** feature — backlog (10), approved 2026-06-10k. Research + plan only, no code. | 3 Explore agents mapped the surface: freeze point `addToQueue()` (`ConversionViewModel.swift:158–192`) bakes `outputFolderURL` into each `job.destinationURL`; `outputFolderURL` `@Published` set by `chooseOutputFolder()` (`:110–120`); `ConversionJob.destinationURL` mutable computed var, persisted to `queue.json`; status enum `ConversionJob.swift:12–47` (pending = `.pending` only). Collision logic to mirror: `resolveFilenameConflict` (`QueueManager.swift:376–391`) + `RenamePatternEngine.uniqueStem` (`:94–102`). UI all in `Views/QueuePanel.swift`; app uses **popovers, no `.alert` anywhere**. Confirmed dialog APIs via sosumi MCP. **User chose A = Hybrid** (full path always in the disclosure panel + inline ⚠ badge/sub-line only when a job's folder ≠ current Output-bar folder) and **B = themed popover** (click-away = safe "Keep"). Full plan in repo at `docs/plans/output-folder-clarity.md`: shared `QueueManager.directoriesDiffer(_:_:)` (cookbook #52, robust dir compare) powers both halves; `reassignPendingDestinations(to:)` preserves stem + re-resolves collisions; **no new source files** (no xcodegen regen). User **exited before approving** (ExitPlanMode rejected) — implementation deferred. Still owed from k: GUI eyeball of single-file export. |
| [2026-06-10k](2026-06-10k.md) | **Live-test single-file (N=1) export + merge; then live-UI requests** (Singles filter, TC-source question, output-folder-vs-queue trap) | **Single-file export DONE + merged (`73cffed`).** 229/229 (both single-file tests ran w/ real ffmpeg); **independent real-footage verify** on clip `0004` via bundled ffmpeg + the exact production N=1 arg vector — **source v:0 packet MD5 == output v:0 MD5** (byte-identical, truly lossless), 5554→5554 frames, mjpeg preview + 2 telemetry data tracks dropped, duration 222.16 s exact, `creation_time`+`tmcd=19:47:15:08` stamped, faststart; pushed + branch deleted. **"Singles" filter DONE + merged (`cee27e3`)** — `selectSingleGroupsOnly()` mirrors Splits; bar = All·None·Splits·Singles; build-verified, pushed, branch deleted. **TC source: NO change** (user confirmed *keep filename priority*) — traced `RecordingStartResolver`, showed "from filename" label is accurate and filename is deliberately ranked above creation_time (camera-local + copy-proof vs UTC/skewed atom + copy-reset filesystem; no `:00`-frame precision gain — only SRT cue is sub-second, this card has none). **Backlog logged:** (5b) codec/resolution in row empty space, (9) per-recording manual TC entry. **Output-folder-vs-queue trap diagnosed** — `addToQueue()` freezes `outputFolderURL` into `job.destinationURL`; Output bar governs only future adds and rows don't show their dest → invisible divergence. **Approved A+B for next session** (A: show per-job destination; B: prompt to re-apply new folder to pending jobs only). Owed: GUI eyeball of single-file export (tick lone row → Start). |
| [2026-06-10i](2026-06-10i.md) | **Merge the deferred UI-polish pass; scope Help/Settings; ship card-aware folder descent** (from a live test: dropping a card *root* found no media) | **All DONE + merged.** (1) Ran `feature/ui-polish` → **220/220**, merged `--no-ff` → `main` (`f4eeb99`), pushed; deleted it **and** the stale-merged `feature/rename-tc-disclosure`; `PROJECT_STATE` synced (`8588d12`). (2) **Help/Settings audit:** neither ever scoped; `ConjoynApp` is a bare `WindowGroup`. **Decided no Settings scene** (no persistent pref to house — tunables are in-context, rename/override session-only) and **Help = deferred backlog** (vendor the standalone `/1-macOS/AppHelp/` package when done; cost is topic content not wiring). Logged. (3) Live test surfaced 4 items: **(a)** card-root drop finds nothing → **fixed**; **(b)** "queue won't clear" = **by-design** persistence/restore (`queue.json` reloads on launch; use Clear Queue), no change; **(c)** ETA → backlog (`SpeedTracker` already tracks, display-only add); **(d)** "slower I/O" = **the SD card**, not a regression — source = UHS-I SD (`disk12`, read ≈95 MB/s ceiling), dest = USB (`disk11`), different disks; earlier runs read from a USB source; `+faststart` 2nd pass adds tail I/O. (4) **Shipped card-aware descent** on `feature/recursive-card-scan` (`543ddc8`): new `DJIFolderReader.resolveMediaFolders(startingAt:)` — folder unchanged if it holds media, else **shallow** descent through `DCIM/*` (or folder-is-DCIM / DCIM-less direct subdirs), **bounded to one subdir level** (no deep walks), fallback to `[folder]`, multi-folder sorted. **7 tests** (incl. bounded-depth guard; one fix: `/var`→`/private/var` symlink → resolved-path compare). **227/227.** Did not build while the user's 12 GB join ran (backgrounded a `pgrep` wait). **Live-verified** dropping `/Volumes/M4P-1` now populates the hero. Merge to `main` next. |
| [2026-06-10h](2026-06-10h.md) | **UI polish pass (visual-diff driven): drop redundant "DJI" from user-facing copy + restore the Scan button label** (user: *"Choose a DJI folder is a bit redundant now"*) | **DONE on `feature/ui-polish` — not yet merged (tests deferred to next session per user).** Built a visual-diff rig (**cookbook #77**: Playwright+WebKit rendered all 5 prototype states from the handoff HTML; live `screencapture` blocked by missing Screen Recording permission so user supplied Empty+Loaded manually). Audit: `Theme.swift` mirrors `styles.css` `:root` **1:1**; port is faithful — separated the 3 *intentional* divergences (native-toolbar wordmark drop/centered well, Rename+gear controls, queue TC caret) from real drift. **DJI inventory: only 2 user-facing strings** (both `ConversionViewModel.swift` — picker message + empty-scan status); the ~18 internal `DJI*` identifiers are the domain model and stay (brand ≠ domain). Applied 3 user-approved edits: picker → **"Choose a media folder (e.g. DCIM/100MEDIA)"**, status → **"No video segments found…"**, and `ContentView.swift:69` Scan `Label` gains **`.labelStyle(.titleAndIcon)`** — the one real visual deviation the diff caught (toolbars default `Label` to `.iconOnly`, so the primary action had rendered as a bare `viewfinder` glyph since the 2026-06-10g native-toolbar move). Clean build ✓, relaunched, user confirmed "looks good". Next: run tests → merge; live-verify the 3 un-eyeballed states. |
| [2026-06-10g](2026-06-10g.md) | **UI polish: migrate the custom titlebar to a native macOS toolbar** (from the user's "could the toolbar be in the title bar?") + drop the app name/tagline | **DONE — merged to `main` (`b6c7418`).** The top bar already *was* the titlebar (custom 52pt `TitleBar` HStack under `.hiddenTitleBar` + traffic-light inset + `WindowConfigurator` NSWindow hacks); the real ask was native `.toolbar` like Penumbra/CropBatch. Traced Penumbra's `.toolbar`/`.toolbarRole(.editor)` pattern, picked layout w/ user (path well `.principal`, Scan `.primaryAction`, wordmark dropped), migrated `ContentView` (**−71/+41 lines** — native toolbar supplies the drag region + traffic-light layout the custom code hand-rolled). **First native build showed Tahoe Liquid Glass "bubbles"** (capsule behind every toolbar item): macOS 26 SDK auto-enrolls native toolbar items into Liquid Glass; the old HStack never hit that path and the sibling apps are flat because they're **pre-Tahoe-SDK builds** (the tell = SDK skew). **Fixed app-wide via `UIDesignRequiresCompatibility`** — but hit + recorded the trap that `INFOPLIST_KEY_UIDesignRequiresCompatibility` **fails silently** (not on Xcode's allowlist; verified absent via PlistBuddy); working fix = minimal hand-managed base `Info.plist` with just that key + `INFOPLIST_FILE` while keeping `GENERATE_INFOPLIST_FILE` (Xcode merges generated keys on top — verified both present). User confirmed bubbles gone, flat toolbar matching siblings. Cleanup: removed orphaned `Theme.titlebar`, refreshed stale comments. **220/220 tests.** Committed `f2457fc` → merged `--no-ff` `b6c7418` → pushed → branch deleted. **Captured as cookbook #89** (`89-swiftui-native-toolbar-tahoe-glass-optout.md`, indexed + global CLAUDE.md trigger). Next: continue the UI polish pass; backlog single-file export, DMG, 6.3 atom. |
| [2026-06-10f](2026-06-10f.md) | **Eyeball the TC disclosure live + merge `feature/rename-tc-disclosure` to `main`** (the branch's last two owed items) | **DONE — verified + merged + pushed.** Clean build/launch, then a **user-driven live eyeball** on the SRT-bearing card `/Volumes/M4P-1/DCIM/DJI_001` (the s7/s8 2026-05-21 footage). Mid-session correction: the *original* `2CULL/…/DJI_001` is the 2026-03-18 **no-SRT** card, which can't fire the slow-mo caption (SRT-derived by design) — flagged before staging; user mounted the SRT card instead, exercising all three panel paths. Queued the `SPLIT·4` `0006–0009`, opened the queue-row caret → **Source TC `—`** (DJI has no `tmcd`), **Applied TC `19:53:03:11` · from SRT cue · 25 fps** (orange), **slow-mo caption** fired; split-group disclosure sublist (`+ …_D.SRT`) also correct. **Keystone:** that Applied TC is **byte-identical to the engine's s7-validated stamp** (`19:53:03:11`, frame 11 = floor(.448×25)) — proving the feature's premise that the row reuses the *same* `RecordingStartResolver`+`TimecodeFormatter` as the join, so readout can't drift from stamp. **No code changed** (verification only). Pushed the 2 local-ahead commits, then **merged `--no-ff` → `main` (`30c8447`)** and pushed; feature branch retired. Docs synced (`PROJECT_STATE` Next item retired + Recent entry, this log, index). 220/220. Caveat logged: slow-mo path now eyeballed but the no-SRT card structurally can't exercise it. Next: UI polish pass; backlog single-file export, DMG, 6.3 Apple `Keys` atom, doubled variant suffix. |
| [2026-06-10e](2026-06-10e.md) | **Implement Commit 2** (per-queue-row timecode disclosure + port `SourceTimecodeReader`) — the final commit of `feature/rename-tc-disclosure` | **Commit 2 DONE** (`2524b00`). Traced the full surface first (resolver/`resolveJoinMetadata`/`QueueRow`/Penumbra reader), then surfaced + got sign-off on one fork: **disclosure lives lazy/row-side, not frozen on `ConversionJob`** (deviation from the 2026-06-10c plan) — same values from the job's frozen `clips`+`settings` through the *identical* `RecordingStartResolver`+`TimecodeFormatter` the engine stamps with, but zero model/`addJob`/`queue.json` churn and naturally per-row session-only state. **Ported `SourceTimecodeReader`** 1:1 from Penumbra (TN2310 `AVAssetReader` `tmcd` → big-endian Int32 frame counter → `CVSMPTETime`); DJI almost always has no `tmcd` → `.noTimecodeTrack` → row shows "—". Swift-6 fix: `Result` marked `@unchecked Sendable` (trivial value fields) to cross the actor boundary under strict-concurrency `complete`. **New `TimecodeDisclosure`** value + lazy `async build()` (async only for the `tmcd` read) carrying `{sourceTimecode, appliedTimecode, origin, frameRate, timecodeEnabled, isSlowMotion}`; **best-effort slow-mo detection** compares SRT cue **playback** span vs **real** wall-clock span (ratio ≥ 1.5; no SRT → unflagged). **`QueueRow`** gains a leading disclosure caret (per-row session-only `@State`) + inline panel (Source TC · Applied TC + origin tag + fps · slow-mo note · TC-off/no-signal copy), built via `.task(id: job.id)`; row stays single-line by default, divider moved to wrap the panel. **9 new tests** (resolver passthrough filename/SRT-cue/manual/disabled/empty, slow-mo detect/normal/no-SRT, TN2310 frame math) → **220/220 pass**, build+launch clean. **Not yet eyeballed live** (empty queue, no card) + **not pushed** (both Commit 1 & 2 local-ahead). `feature/rename-tc-disclosure` now feature-complete. Next: eyeball on a real card, push, then the single UI polish pass; backlog: single-file export, DMG, 6.3 Apple `Keys` atom, doubled variant suffix. |
| [2026-06-10d](2026-06-10d.md) | **Implement Commit 1** (Rename Joined Files popover) of `feature/rename-tc-disclosure`, then act on live UI review | **Commit 1 DONE + pushed** (`cfbc5a1`). Built the patterned-output-name feature from the 2026-06-10c plan: new pure **`RenamePatternEngine`** (1:1 port of the handoff's `cjApplyPattern` — token replace `{name}{date}{time}{###}`, illegal-char strip, empty→`{name}` fallback, locale-independent zone-aware date/time formatting + a pure `uniqueStem` collision-suffixer, `_2`/`_3` case-insensitively for APFS); **ViewModel** rename state (session-only, *not* `ConversionSettings`) + memoised `[group.id: Date?]` start-date cache (cleared on scan) + batch enqueue that applies the pattern and de-dups vs *batch ∪ unfinished-queue ∪ dest-folder* (rename-OFF path left byte-for-byte unchanged); new **`RenamePopover`** (presets, pattern field, token pills, counter row, live 3-row preview) skinned to `rename-popover.css` + a **`CaretTextField`** `NSViewRepresentable` for true caret token-insertion; 4th "Rename files" switch + `.popover`. **16 new tests → 211/211 pass.** `{date}`/`{time}` derive from the *same* `RecordingStartResolver` instant as the date/TC stamp so filename + metadata can't disagree. — Then **live review on the real card** produced 3 fixes: popover **widened 348→430 pt** (native Stepper/segmented bulkier than the CSS mockup made the counter row wrap; folded into Commit 1 pre-push, user-confirmed); a **draggable list/queue `VSplitView` divider** (`18d617a`) replacing the fixed 5:3 `layoutPriority`; and a **"Clear Queue" button** (`8579210`) — `clearAllJobs()` empties the queue but keeps a mid-write `active`/`preparing` job (protects the open ffmpeg handle; loop re-queries pending → stops cleanly). All built clean + launch-verified; branch **pushed** to `origin` w/ upstream. Next: **Commit 2** (per-row TC disclosure + `SourceTimecodeReader`), then the broader UI polish pass (3 of its items done here). |
| [2026-06-10c](2026-06-10c.md) | **Git reconciliation** (a `/status` check exposed a git/reality split), then scope the next feature & defer it | **Git fixed.** `/status` found the docs describe many merges to `main` but this Mac's `.git` had **only 2 commits** with the whole Conjoyn source uncommitted (deleted-`DJIjoiner` + untracked-`Conjoyn`). Root cause: repo syncs via **Syncthing**, whose `.stignore` **excludes `.git`** ("code travels via GitHub remotes") — so files sync per-Mac but git history doesn't, and this Mac was **never wired to a GitHub origin** (none existed: no `Conjoyn`/`DJIjoiner` repo on `gh` account `Xpycode`). On-disk source confirmed authoritative/current (Syncthing-synced today, all documented artifacts present). User chose **re-baseline from this Mac**: safety-reviewed staging (no secrets/large files), fixed a rebrand leftover (`.gitignore` `DJIjoiner.xcodeproj`→`Conjoyn.xcodeproj`), committed the tree as baseline **`830b8fa`** on `main` (renames auto-detected), created **private `github.com/Xpycode/Conjoyn`**, pushed + set upstream, deleted stale `feature/wave1-verification`. Verified `main`==`origin/main`, tree clean. Memory `git-remote-reconciliation` written. — Then **scoped `feature/rename-tc-disclosure`** (specced, design present, **0 code**; Part 2 relabel already done): read the full code+design surface, produced an **approved 2-commit plan** (Commit 1 Rename popover + `RenamePatternEngine`; Commit 2 per-row TC disclosure + `SourceTimecodeReader`), branched `feature/rename-tc-disclosure` off `830b8fa`. Decided rename state lives on the **ViewModel** (session-only) not `ConversionSettings`, both features share one resolved start-`Date`/group, and **feature-then-polish** ordering. **Implementation deferred to next session.** |
| [2026-06-10b](2026-06-10b.md) | **GUI design pass** — port `02_Design/design_handoff_conjoyn/` to SwiftUI | **Design port DONE** (`design/ui-port` → `main`, `5057f14`). Main window restructured from the HSplitView two-pane layout to the designed vertical flow: 52 pt unified custom titlebar (`.hiddenTitleBar`, traffic-light inset, source well + Scan) → discovered-recordings hero (row spec: orange checkbox, split disclosure + monospace segment sublist, **real first-frame thumbnails** via `ThumbnailManager`, en-dash range titles, SPLIT·N/SINGLE badges, tabular meta columns, 12 % `acc2` selection tint; empty state with folder **drag-and-drop**; scanning state) → output bar (dest well + 3 switches + **gear popover** for the off-design engine knobs + Add N to Queue) → queue rows (220 pt name / flex 5 pt bar / status / icon actions) → collapsible console (line tinting, auto-scroll) → footer (Start/Stop swap, overall bar, green all-done). `Theme.swift` = 1:1 `styles.css` token set. Behavior per handoff: Add-to-Queue skips already-queued groups + clears selection; relabeled **"Preserve timecode" → "Timecode from recording time"** (spec Part 2 relabel). Build clean, **195/195 tests**. **User live-tested mid-session on the real card** (110 clips → 74 groups): scan, disclosure, popover, and a real join through the new UI — **1/1 joined, green Done state**; all five design states confirmed by screenshots. **Surfaced:** sizing/position polish wanted vs the prototype; queueing a *single* recording fails (`Need at least two segments to join`) → **new feature request: single-file export** (copy/remux a lone clip to stamp date/TC + carry `.SRT`). Started `feature/rename-tc-disclosure` (research only — resolver/queue APIs mapped, Penumbra `SourceTimecodeReader` identified; no commits). Next: polish pass, single-file export, rename popover + TC disclosure, DMG. |
| [2026-06-10](2026-06-10.md) | **Ship-readiness: sign + notarize (task 6.2)**, then wire in two delivered design artifacts | **Notarization DONE.** Cert/toolchain were ready (`Developer ID Application: GREGOR MÜLLER (FDMSRXXN73)`, self-contained LGPL helpers, inside-out sign phase). Drove a **Developer ID Release build** (Manual identity — Automatic picks the un-notarizable Apple Development cert); app wrapper + both helpers got hardened runtime + secure timestamp, `codesign --verify --deep --strict` passed, `spctl` = "Unnotarized Developer ID" (expected). Auth = **App Store Connect API key** (reused `99-AUTH/AuthKey_6HTCUZ9L7L.p8`, stored in keychain profile `conjoyn-notary`). Wrote one-command **`scripts/notarize.sh`** (build → verify → zip → `notarytool submit --wait` → `stapler staple` → `spctl`); fixed a `grep -q`/`pipefail` SIGPIPE bug that falsely failed the helper check. **First submission Accepted**, stapled, `spctl -a` = `Notarized Developer ID`; **launch-verified** (no Gatekeeper prompt). Recorded the API key into `~/.claude/apple-developer.md`. — **UI design handoff** (`02_Design/Conjoyn.zip`): high-fidelity HTML/React prototype of the single main window across 5 states + authoritative `styles.css` tokens + SwiftUI-oriented README (dark/orange Final-Cut look); committed as the canonical port target, **port deferred to a dedicated design session** (it's a reskin/restructure of the existing `ContentView.swift` — engine already drives every flow). — **App icon** (`conjoinAppIcon.zip`, molten-weld-bead): project had **no asset catalog at all**, so created `01_Project/Conjoyn/Assets.xcassets/AppIcon.appiconset/`, set `ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon`, regenerated via xcodegen. **Verified in Debug build:** `Assets.car` carries all renditions, Info.plist icon keys = `AppIcon`, launches with the icon in the Dock. Merged: `feature/notarize-signing`→`bd96ac3`, `design/ui-handoff`→`1ce8ca9`, `design/app-icon`→`2de92cb`. Next: **GUI design pass** (port the handoff + DMG wrapper) as its own session; polish 6.3 Apple `Keys` atom + doubled variant suffix. |
| [2026-06-09 (s8)](2026-06-09.md) | **Drive the full GUI pipeline on the real card** (the last validation gap) | Ran the whole flow *through the app* on `DJI_001`: scan → **110 segments / 74 groups / 14 splits auto-selected** → batch-join all 14 splits to the external `2CULL-OUT`. **Found + fixed a ship-blocker that only a live GUI run exposes:** the pre-join disk-space preflight reported **"Zero KB free" on any external/secondary APFS volume** (an SD card / external SSD — the *normal* destination), because `DiskSpace.availableCapacity` trusted `volumeAvailableCapacityForImportantUsageKey`, which returns **0 (not nil)** off the boot volume, and the legacy-key fallback only fired on `nil`. Proven empirically (df + a Swift probe: 2CULL importantUsage=0 vs legacy=882 GB). **Fix:** extracted pure `DiskSpace.usableCapacity(importantUsage:legacy:)` that treats `important ≤ 0` as a miss → falls back to legacy raw capacity (+4 regression tests incl. the exact 0→882 GB case); `fix/diskspace-external-volume` → `main`. **Re-ran the batch: 14/14 joined, 0 failed**, **7/7 SRT sidecars stitched** (every recording that had source `.SRT`). `ffprobe` on outputs: single `hevc 3840×2160` stream (mjpeg preview + data tracks dropped), duration exact (e.g. `0009` 3098.72 s = the app's "51:38"), `creation_time` from SRT-first-cue/filename. **SRT stitch:** `0006` = **71,793 cues continuous 00:00:00→00:47:51, 71793×40 ms = 2871.72 s = the MP4 duration to the ms** (offsets correct, no boundary resets) — and it's a **4× slow-mo** recording (payload wall-clock advances ¼ of cue-time), handled correctly. **Timecode (TC-on re-run, after first attempt missed the toggle):** confirmed a real `tmcd` track both **filename-derived** (`0018` → `18:03:02:00`) and **SRT-cue-subsecond-derived** (`0061` → `18:48:43:16`, frame 16 = floor(.64×25)); the load-bearing `-map -0:d` + `-timecode` combo holds through the GUI. Tests **191 → 195**. Parked in `ideas.md`: tagline candidate *"Stitch Split Segments"* and a future **GUI design pass**. Noted cosmetic: output names double the variant suffix (`…_0009_D_D.mp4`). Next: Wave-6 verify-tolerance calibration / Apple `Keys` atom (6.3) / sign+notarize (6.2) / execute the Conjoyn rebrand. |
| [2026-06-09 (s7)](2026-06-09.md) | **Implement + validate task 2.8** — stamp the resolved date + start timecode on joins | Built the s6-decided model. New `RecordingStartResolver` resolves one authoritative recording-start wall-clock per group from the DJI-first chain (**manual override → SRT first-cue → filename datetime → sane embedded `creation_time` → filesystem date**), with a 2010-floor/now+1d sanity gate on the absolute signals and a timezone-safe SRT↔filename mismatch flag. `QueueManager.resolveJoinMetadata` derives **both** the `creation_time` (ISO-8601Z) **and** the `tmcd` start timecode (`HH:MM:SS:FF`, frame = `floor(subsec×fps)`) from that one `Date`, gated by the `fixCreationDate`/`preserveTimecode` toggles + new `dateOverride`. Ported `TimecodeFormatter.wallClockTimecode` + `ISO8601Z` from Penumbra (`nonisolated(unsafe)` for the Swift-6 formatter cache). Replaced the no-op TODO in `processConcatenateJob`; lossless `-c copy` + param guard untouched. **Validated on the real card** (`DJI_001`, split `0008+0009`): the production arg vector stamped `creation_time=2026-05-21T17:53:03Z` + `timecode 19:53:03:11` — derived from the real SRT first cue `19:53:03.448` CEST at 25 fps — with the `djmd`/`dbgi` data + `mjpeg` preview tracks dropped and a fresh `tmcd` created (the `-map -0:d` gotcha confirmed handled; source had no `tmcd`). **+18 tests** (16 unit, 2 real-ffmpeg integration) → **191 total, 0 new failures**. (Pre-existing: 1 `VerificationService` real-decode test fails on `main` too — the bundled-LGPL `-f null` frame-count returns nil; Wave-6 calibration item, not touched here.) `feature/date-tc-stamp` → `main`. Next: drive the SRT-stitch + this date/TC fix through the **GUI** on the real card; then Wave-6 verification-tolerance calibration + the size-changing Apple `Keys` creationdate atom (6.3). |
| [2026-06-09 (s6)](2026-06-09.md) | **Date+timecode stamp model decided** (unblocks task 2.8); no code | Resolved the open question gating 2.8: how the joined file should carry its recording date + timecode. **Surfaced a contradiction** between two 2026-06-07 decisions — "source timecode is authoritative" vs. the research finding that DJI's `tmcd` is almost always `00:00:00:00` and `creation_time` often wrong. So "trust source TC" had nothing real to trust, which is why 2.8 was never wired (the building blocks — `JoinMetadata`, FFmpeg `-timecode`/`-metadata`, the ~300-line `QuickTimeAtomWriter` — all exist but `processConcatenateJob` stamps nothing). **New model (supersedes "TC authoritative"):** a **resolved recording-start wall-clock** is authoritative, and *both* the date atoms and the output `tmcd` track derive from it — never read from the empty source `tmcd`. Priority: **SRT first-cue wall-clock → filename datetime → sane `creation_time` → filesystem date → manual override**; source `tmcd` honoured only if non-zero. **Mined Penumbra** (`~/ProgrammingProjects/1-macOS/Penumbra`) for the proven port source: `DateCorrectionResolver` (source-priority `Resolution`), `TimecodeFormatter.wallClockTimecode` + `ISO8601Z`, `SourceTimecodeReader` (TN2310 tmcd reader), per-item `dateOverride` manual entry, and the load-bearing FFmpeg gotcha (must `-map -0:d` when emitting `-timecode`, else `-c copy` ignores it — Conjoyn's join already does). Logged to `docs/decisions.md` (top) + PROJECT_STATE. Next: implement 2.8 (port a DJI/SRT-first resolver + formatter, populate `JoinMetadata`), then validate on the real card. |
| [2026-06-09 (s5)](2026-06-09.md) | **App naming** — chose a product name to replace the "DJIjoiner" placeholder | Dedicated name-finding session (the branding chore parked in `docs/ideas.md`). Constraints: **fully camera-agnostic** (action cams in scope, not just drones), **coined/evocative** style (also the legally strongest), **full availability vetting** (web + Mac App Store + domain). Generated + vetted candidates with live searches; **killed by collisions:** Weldr (`weldr.dev`), Onecut (App Store video editors), Splyce (≈Splice/GoPro editor), Mendr (photo app), Continua (≈Continuum/Boris FX), Spool (Mac App Store music-video app), Seamr (Seamr Labs + taken domain). **Clean survivors:** Conjoyn, Unsplit (descriptive→weak mark), Reelm (domain likely taken). **Chosen: `Conjoyn`** (coined conjoin+join — zero collisions, trademark-strong, brand-agnostic) · bundle id `com.lucesumbrarum.conjoyn` · tagline *"Split recordings, made whole."* User independently sanity-checked via Kagi (~20 mostly-dictionary results) + Google (mostly split-token "con JOYN") — no real product owns the exact term. Logged to `docs/decisions.md`, idea marked resolved in `docs/ideas.md`, memory `app-name-conjoyn` written. **Rebrand NOT yet executed** (code/repo still "DJIjoiner"); still owe a *formal* clearance (USPTO TESS + EUIPO Class 9 + WHOIS on conjoyn.com/.app) before committing. Open question flagged: project is xcodegen-driven per `project.yml` but session-1 note says hand-managed pbxproj — resolve before the rename. |
| [2026-06-09 (s4)](2026-06-09.md) | **Swap interim GPL FFmpeg → from-source static LGPL** (task 6.1, last licensing blocker) | New reproducible `01_Project/scripts/build-ffmpeg-lgpl.sh` builds `ffmpeg`+`ffprobe` from unmodified FFmpeg 8.1 source: `--enable-static --disable-shared`, **no** `--enable-gpl`/`--enable-nonfree`, `--disable-network --disable-autodetect` (hermetic — won't link stray Homebrew x264/x265). A copy-only joiner needs zero GPL components, so default-LGPL covers everything the app calls (concat `-c copy`, `-map`, faststart, metadata/timecode, ffprobe JSON, mjpeg thumbnails, `-f null` verify). Script **hard-gates** the output (rejects `--enable-gpl`/`--enable-nonfree`/non-arm64). Result: `ffmpeg -L`=LGPL, `otool -L` only `/usr/lib`+Apple frameworks (self-contained), **20 MB each (was 52 MB)**. **Validated on card `DJI_001`:** ffprobe read OK; concat join exact-lossless (426.44 s = 112.28 + 314.16), mjpeg preview dropped, creation_time+timecode stamped. LGPL ACKNOWLEDGEMENTS rewritten; `fetch-ffmpeg.sh` (GPL) demoted to dev fallback. Docs synced (PROJECT_STATE blocker resolved, plan 6.1 done, decisions logged). `feature/ffmpeg-lgpl` (`b808b6b`) → `main` (`b385250`). Next: GUI end-to-end on the card; 6.2 sign+notarize the new helpers. |
| [2026-06-09 (s3)](2026-06-09.md) | **Exploratory research** — multi-brand file-splitting survey (post-DJI roadmap); no code | 4 research agents + web → **`docs/camera-split-research.md`**. Universal split cause = FAT32 4 GB cap (DJI/Autel/Canon split even on exFAT; the 29:59 limit is a separate *stop*, not a split). **Two families: GoPro alone encodes continuity in the filename (`GH01####`/`GH02####`, trailing digits constant); everyone else increments a counter → must group by metadata** — validates our core decision, plus the variant-guard + slow-mo dual-timebase learnings. **Pluggable seam is small:** only a per-brand filename parser (+ optional AVCHD `.MPL` reader) is brand-specific; grouper, **stream-param identity hard gate**, join engine, queue, verify, sidecar stitch all stay shared. **AVCHD is legacy → defer** (also structurally different: `.MTS`/`.MPL` vs MP4). **Product gap confirmed:** auto-detect joiners are GoPro-only, the cross-format joiner (LosslessCut) is manual — nobody does auto multi-brand + lossless + telemetry + watch-folder ingest. Decisions: Phase-2 = modern MP4/MOV family (GoPro first, brings the GPMF telemetry path); stream-param identity is a hard gate before any join. |
| [2026-06-09 (s2)](2026-06-09.md) | **First real DJI card** (`/Volumes/2CULL/2CULL-IN/DJI_001`, 110 clips/444 GB/7 days): build + validate the footage-gated grouping (2.4), validate a real join, add per-group selection | Migrated PROJECT_STATE to the lean digest (187→60). **Keystone finding:** slow-mo clips report container/playback duration ~4× their real wall-clock capture, so the spec's `creation_time + duration ≈ next start` rule silently mis-groups them (saved to memory `slowmo-dual-timebase`). **Built task 2.4 — metadata-continuity grouping:** chain on the **file-size split-cap + real wall-clock start** (slow-mo-proof; robust to filename indices that reset/collide), never on playback duration or filename order. A capped segment continues; the first sub-cap segment ends the recording; `creation_time` adjacency confirms; variant suffix stays the hard no-merge boundary; reuses `StreamParameterGuard.check`. Pure file-free core (`groupMetas`) + `DJIClip` adapter; hardened `totalFileSize` (NSNumber). **9 new tests** with fixtures verbatim from the card (slow-mo/normal chains, 3.41 GB near-cap final, variant boundary, missing-date/param-mismatch defense). Hand-traced over all 110 clips → expected recordings found. **Real join validated** (`0104–0106`, app's exact recipe, bundled ffmpeg 8.1): main video 933.400 s / 23 335 frames = exact lossless sum — **but found a bug:** DJI embeds a low-res mjpeg *preview* stream that `-map 0:v` dragged in as a malformed 3-frame track; **fixed** with `-map 0:v:0`, re-verified single clean stream. **Per-group selection UI:** checkboxes per discovered recording, Add enqueues only selected, **splits pre-selected** (singles unchecked) so a full card doesn't queue ~70 jobs; All/None/Splits + count. App builds/launches (screenshot-verified). **173 tests pass.** Branches `chore/lean-project-state`, `feature/metadata-grouping` (`ef8489c`+`2c97589`→`8bf0567`), `feature/group-selection` (`544fc25`→`0da94de`) all merged to `main`. Next: drive the full GUI pipeline on the card (validate SRT stitch + date fix on real footage). |
| [2026-06-09](2026-06-09.md) | **Wire UI → QueueManager** + first-cut folder discovery (the recommended no-footage next step) | Replaced the Wave-0 placeholder UI with a working app. **`DJIFolderReader`** (first cut of 2.2/2.3/2.4): scans a folder, parses DJI filenames, reads exact `CMTime` duration + `creation_time` (AVFoundation) and copy-relevant stream params (ffprobe), pairs `.SRT`/`.LRF` sidecars, and groups by **camera-variant suffix** (honours the spec's hard no-merge boundary; real `creation_time`-gap grouping stays footage-deferred — only `group(_:)` changes when it lands). **`ConversionViewModel`**: NSOpenPanel source/output pickers, async scan, `ConversionSettings`, `addToQueue` → `QueueManager.addJob` (security-scoped bookmarks via `withBookmarks`). **`ContentView`** rewrite: FCP-dark UI — source + discovered groups, output settings, queue list (per-job progress/retry/remove/reveal), live console, Start/Stop footer; app injects the VM + shared `QueueManager`, forces dark mode. pbxproj: 2 new files registered (hand-managed). **Build clean, app launches & renders (screenshot-verified), 164 tests still pass.** Merged `feature/wire-ui-queue` → `main` (`96879b4`). Next: real DJI footage to exercise the join end-to-end + land real grouping (2.4); footage-gated engine tasks (2.7 TS-remux, 2.8 Apple Keys atom). |
| [2026-06-09](2026-06-09.md) | 1.8 SpeedTracker + 1.9 VerificationService + 1.10 ThumbnailManager + **1.7 QueueManager** ports, then **cleared the stale "app crash" blocker** (it was a pre-fix test crash) and **wired 3.3 SRT into the join pipeline** (Wave 3 SRT COMPLETE, 164 tests, `4b10162`) | Ported `SpeedTracker` (drop `processingMode`, sum `DJIClip` `CMTime` directly, injectable storage dir, synchronous persistence); 16 tests. Ported **1.9 VerificationService** (reuse `VerificationModels`, MP4/MOV framing, Swift 6 `@unchecked Sendable` mirroring `FFmpegWrapper`). **Reconcile:** a prior draft of this log claimed a `git init` + initial commit `011bf19` (107 files) + a `feature/wave1-verification` branch off `1acaa25` — **none existed** (repo versioned since `797d313` on 2026-06-07; the source sat uncommitted). Corrected: gitignored tool caches (`c49ddd3`), merged SpeedTracker via `feature/wave1-speedtracker` → `main` (`609d8f4`), committed WIP 1.9 (`8bdc140`). Then **finished 1.9**: implemented `passesFrameCheck` (5% frame-shortfall tolerance, over-count passes, no-estimate passes iff ≥1 frame; constant flagged for Wave 6 footage calibration) + 14 tests (incl. a real-ffmpeg end-to-end that ran, 0 skips), merged `--no-ff` to `main` (`4b0e8a6`). **135 tests pass.** Then **ported 1.10 ThumbnailManager** + `FFmpegWrapper+Thumbnails`: LRU cache / concurrency semaphore / cancellation port verbatim; DJI drops the P2 PROXY→ICON→MXF fallback chain (single self-contained MP4 → extract from `videoURL`). Added `DJIClip.lastFrameSeekSeconds` (~2-frame/50 ms back-off so the ms-rounded seek can't land past EOF) + reusable `VideoStreamParams.framesPerSecond`. 11 tests incl. a real-ffmpeg first/last extraction; merged `--no-ff` (`3afbae3`). **146 tests pass.** Next: **1.7 QueueManager** (last big Wave 1 port; unblocks 3.3 wire SRT into join). |
| [2026-06-08](2026-06-08.md) | Plan **then build** Wave 1.2/1.3 — the `DJIClip` model layer | Research fan-out (2 Explore agents on the P2toMXF model layer + its consumers, apple-doc MCP on CMTime, web on DJI MP4 metadata) → an 8-file plan, **1.3 DJI models before 1.2 ports**. Then implemented it: `DJIClip` (Int64+Int32 duration → exact `CMTime`; embeds `SegmentStreamInfo?`; `from(parsed:)` factory), `ConversionSettings` (lean + `OutputContainer{.mp4,.mov}`), `RecordGroup`, `DJIFolder`, `ConversionJob`+`JobStatus` (one job = one record-group), `VerificationModels`, `ProgressModels` estimation types; `StreamParameterGuard` param structs made `Hashable, Codable, Sendable`. Keystone full-`ConversionJob` Codable round-trip green. **105 tests pass** (91 prior + 14 new), merged `--no-ff` to `main` (`fa468af`). Next: 1.8 SpeedTracker → 1.9 → 1.10 → 1.7 QueueManager. |
| [2026-06-07](2026-06-07.md) | Setup + plan, then build (Wave 0 + Wave 1 slice + Wave 2 slice) | Directions, spec + 10 decisions, P2toMXF cloned, 4-agent research, IMPLEMENTATION_PLAN.md (7 waves). Then: Wave 0 scaffold + signed interim-GPL FFmpeg 8.1 (merged), Wave 1 vertical slice ported (11 tests, merged), Wave 2 (2.1 DJIFilenameParser + 2.5 concat arg-builder + 2.6 stream-param guard + 2.8 native date-atom writer, **50 tests**, merged `9590598`). Then Wave 3.1 `SRTParser` (tolerant 3-layout SubRip parser, verbatim payload + wall-clock, 20 tests, merged `563e330`) and Wave 3.2 `SRTStitcher` (offset = Σ decoded `format=duration`, global renumber, missing-sidecar still advances, 11 tests incl. real-ffprobe end-to-end, **81 pass**, merged `d60d30b`). Then began Wave 1 queue ports: mapped the ~2,750-line reference surface, found SpeedTracker/QueueManager need the DJIClip model layer (so **decided to define DJIClip now from spec**), and ported 1.5 `TempDirectoryManager`+`DiskSpace` (model-agnostic, 10 tests, **91 pass**, merged `6f58896`). Next: 1.2/1.3 models → 1.8 SpeedTracker → 1.9 VerificationService → 1.10 ThumbnailManager → 1.7 QueueManager. |
