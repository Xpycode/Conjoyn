# Session: 2026-06-10l

## Goal
Plan the **output-folder ↔ queue clarity (A+B)** feature — backlog item (10), approved in 2026-06-10k.
Research + plan only; no code.

## Progress
- Fanned out 3 Explore agents over the queue/output surface and built a precise file:line map:
  - **Freeze point:** `addToQueue()` (`ConversionViewModel.swift:158–192`) bakes each job's
    `destinationURL` from the then-current `outputFolderURL`; `outputFolderURL` is `@Published`
    (`:29`), set by `chooseOutputFolder()` (`:110–120`).
  - **Job model:** `ConversionJob.destinationURL` is a mutable computed var backed by a path
    string, persisted in `queue.json`; status enum `ConversionJob.swift:12–47` (`isFinished` =
    completed/failed/cancelled; "pending/unstarted" = `.pending` only).
  - **Collision logic to mirror:** `resolveFilenameConflict` (`QueueManager.swift:376–391`,
    ` (n)` suffix) + `RenamePatternEngine.uniqueStem` (`:94–102`, case-insensitive).
  - **UI:** `OutputBar` + `QueueRow` + `TimecodeDisclosurePanel` all in `Views/QueuePanel.swift`;
    app uses **popovers, no `.alert`/`.confirmationDialog` anywhere**.
- Confirmed dialog APIs via sosumi MCP (`confirmationDialog`/`alert`, both macOS 12+ — fine on the
  14+ target).
- Asked the two genuine UX calls; user chose **A = Hybrid** (full path always in the disclosure
  panel + an inline ⚠ badge/sub-line only when a job's folder ≠ the current Output-bar folder) and
  **B = custom themed popover** (click-away = the safe "Keep").
- Wrote the full plan to `~/.claude/plans/merry-painting-toast.md`. User chose to **exit before
  approving** (ExitPlanMode rejected) — implementation deferred to next session.

## Decisions
- **Keep per-job destination freezing; fix is feedback, not re-architecture.** A=transparency,
  B=explicit one-click re-point of `.pending` jobs. (Tier C — pending jobs live-track the folder —
  stays deferred.)
- **One shared `QueueManager.directoriesDiffer(_:_:)`** (symlink-resolved, standardized,
  case-insensitive; nil-safe) powers both the badge (A) and change-detection (B) — the robust seam
  per cookbook #52 (URL directory equality is finicky).
- **Plan needs no new source files** → no xcodegen regen; all edits land in existing files
  (`ConversionViewModel.swift`, `QueueManager.swift`, `QueuePanel.swift`, existing test file).
- SRT sidecar redirects for free (path derived from `destinationURL` at process time).

## Next
- Implement A+B per `~/.claude/plans/merry-painting-toast.md` on branch
  `feature/output-folder-clarity` off `main`:
  1. `QueueManager`: `directoriesDiffer` + `reassignPendingDestinations(to:)` (preserve stem,
     re-resolve collisions vs non-pending jobs + filesystem; refresh/clear stale
     `outputBookmarkData`; `saveQueue()` + log).
  2. `ConversionViewModel`: change-detect in `chooseOutputFolder()`, `showApplyFolderPrompt`,
     `pendingJobCount`, `applyOutputFolderToPendingJobs()`.
  3. `QueuePanel`: `ApplyFolderPopover` on the Output bar; `QueueRow` inline ⚠ badge + sub-line
     (`folderMismatch`, `.pending` only); disclosure "Output" row.
  4. Tests for `directoriesDiffer` + `reassignPendingDestinations` (existing test file).
- Still owed from 2026-06-10k: **GUI eyeball of single-file export** (tick a lone row → Start).

## Handoff (switching Macs)
- Plan copied into the repo at **`docs/plans/output-folder-clarity.md`** (the
  `~/.claude/plans/merry-painting-toast.md` original is local-only and won't travel — Syncthing
  excludes `.git` and never sees `~/.claude`).
- Doc changes committed + **pushed to `origin`** so history travels (not just the Syncthing'd
  working tree). **On the other Mac, before any work:** `git fetch origin && git reset --hard
  origin/main` to align `.git` with the synced files (per memory `git-remote-reconciliation`).
- No code was written, no branch created yet — start `feature/output-folder-clarity` off `main` on
  the other Mac when implementing.
