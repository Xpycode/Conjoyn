# Session: 2026-08-10

## Goal
Cut and ship **1.0.5 / build 105** — the release that takes the whole GoPro camera family
(waves G0–G8) and the renamed-footage parser fix public.

## Progress
- **Version bump:** `project.yml` 1.0.4/104 → 1.0.5/105, `xcodegen generate`, verified in the
  generated pbxproj. Committed `b98de2a`.
- **Suite green pre-cut: 618 / 0 fail.** First run's output was inconclusive (the `-quiet` pipe
  swallowed the summary and masked the exit code behind `tail`), so it was re-run with `pipefail`
  + a result bundle rather than trusted — second run produced the explicit
  `Executed 618 tests, with 0 failures` + `** TEST SUCCEEDED **`.
- **make-dmg.sh:** both notary round-trips **Accepted**, app + DMG stapled, Gatekeeper
  `accepted — Notarized Developer ID`. Exported app independently confirmed 1.0.5 (105) and
  stapled before the DMG step.
- **Release notes written before `make-appcast.sh`** (that ordering is what emits the
  releaseNotesLink), carrying both owed lines: downgrading to 1.0.4 clears the queue, and GoPro
  joins from ≤1.0.4 carry a start timecode up to ~0.7 s early — re-join to correct.
- **Appcast:** signed and self-verified (sparkle:version 105, exact enclosure length, EdDSA
  signature). Now serves 1.0.5 + 1.0.4 + 1.0.3 — `generate_appcast` rotated 1.0.2 out on its own
  (a rolling 3-item window) — and emitted **two** deltas, one per older DMG still staged:
  105→104 (264 KB) and 105→103 (298 KB). Both deployed with the DMG.
- **Deploy:** DRY_RUN preview first, then live. Verification fetched everything back: appcast,
  notes page and both deltas `cmp`-identical to the staged files; the DMG sha256-identical via
  all three routes (`Conjoyn-1.0.5.dmg`, `dl.php?app=conjoyn`, `downloads/conjoyn.dmg`).
  Website badge bumped to v1.0.5. App-Websites commit `c66d76c`.
- **User eyeballed the real Sparkle update from installed 1.0.4 → offered + worked**, notes
  rendered in the dialog, About shows 1.0.5 (105) after install.
- **User follow-through (own commits):** `noindex` meta on the release-notes pages + portal
  catalogue, deployed (App-Websites `88ac3e5`); staged 1.0.5 notes copy mirrored to match.
- Docs synced along the way: `65a3aae` (PROJECT_STATE ship entry), `f602c5c` (eyeball recorded).

## Decisions
- None architectural — release execution. The operational learning (appcast keeps a rolling
  3-item window; one delta per older DMG still staged, all must ship together) is recorded in
  PROJECT_STATE → Infrastructure.

## Next
- **Capture footage:** the G8.1 Hero 11 **H.264** clip (~30 s → `GH…` name) — the only open
  task in the plan — plus Osmo Action 1 and GoPro 7 material for the next camera families.
- Backlog unchanged (recordings-list search/filter, output-name re-ingest guard, SD-card photo
  preservation, i18n).
