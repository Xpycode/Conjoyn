# 2026-06-24 — Wave 5 closed: real removable-SD-card eyeball (5.14) PASSED

**Mac:** M4 Pro (`Mac16,8`) · **Branch:** `main` · **No app code changed** (test + docs only) ·
**Tests:** 455 app / 1 skip / 0 fail (unchanged) · shipped **1.0.2 / 102** untouched.

## 0. `/status` — reconciled a Syncthing phantom first

Session-start pre-flight: `origin/main` was **1 commit ahead** (`cd86097` — the 2026-06-23 session
log) and the working tree showed 3 "changes" (`PROJECT_STATE.md`, `_index.md`, untracked
`2026-06-23.md`). Verified each on-disk file against `origin/main`'s blob with `git hash-object`:
**all three byte-identical** — a pure Syncthing shadow of the already-pushed commit (the untracked
`2026-06-23.md`'s `git diff` "71 deletions" was a representational artifact — `git diff <commit>`
can't see untracked files, so it reports the tracked side as absent, not a real content delta).
Lossless `git reset --hard origin/main` → `main == origin/main`, clean. No duplicate commits.

## 1. The 5.14 test — real removable SD card

**Precondition (the one `2CULL` failed):** the card mounted at `/Volumes/M4P-1` is in the
**built-in SDXC reader**, `diskutil info` = `Removable Media: Removable`, ExFAT, with real DJI
footage in `DCIM/DJI_001/` (modern `DJI_YYYYMMDDHHMMSS_NNNN_D` naming + `.SRT` sidecars, one
4-segment split set + singles).

**Setup:** clean Debug build (`xcodegen generate` → `xcodebuild … -configuration Debug` → `open`,
fresh-binary mtime+PID verified — not a stale `_run` snapshot). Bundled ffmpeg+ffprobe present and
fresh, signed with `cs.disable-library-validation` (team `FDMSRXXN73`). **Reset removable-volume
TCC** (`tccutil reset SystemPolicyRemovableVolumes com.lucesumbrarum.conjoyn`) so the prompt would
fire fresh. Backed up + cleared two **stale `2CULL` watch entries** from `UserDefaults`
(`watchFolder.entries`) for a clean slate (backup in scratchpad; restore = `defaults import`).

**First run — PASSED.** Added `/Volumes/M4P-1` as a watch folder (output to an internal Desktop
folder, off-card). **6 DJI groups** discovered → joined `-c copy` → SRT-stitched → fast-verified;
the 4-segment set stitched to a single **13.46 GB** file. `=== Queue processing complete ===`,
**0 failures**, timecode write-back matched, card originals (9 source `.MP4`) untouched.

**TCC finding — the prompt never fired, and that's correct.** Picking the folder through the
`NSOpenPanel` ("Add Folder") is the **powerbox**: macOS treats it as explicit consent and grants
access to that path directly, with **no `NSRemovableVolumesUsageDescription` prompt** even on a
removable volume. That key gates *programmatic / background* access, not user-selected paths —
`WatchFolderBookmark.swift` documents exactly this. So no prompt on add = expected.

**Relaunch — PASSED.** Quit fully (Command-Q) → relaunched the same build. The watch entry + plain
bookmark + `ProcessedGroupLedger` (6 fingerprints, persisted under
`Application Support/Conjoyn/WatchFolders/<uuid>/processed_groups.json`) all survived:
- (a) **no permission re-prompt** — the grant persisted across relaunch for the background read;
- (b) watch **resumed from the bookmark** without re-picking (the panel window is summoned from the
  menu by design, but the entry was active);
- (c) **queue stayed empty — no re-join** (the dedup ledger held; `diagnostic.log` grew 0 lines,
  0 new ffmpeg joins, 0 `EPERM`).

**Live-detection drop-test — PASSED (the decisive one).** To prove the relaunched watcher is
*genuinely live* (not just UI-restored) and that **background** card reads work — the real concern
behind the engine review's "silent watcher death" finding — copied a source clip onto the card
under a fresh index (`DJI_20260524021500_0099_D.MP4`+`.SRT`). With no panel pick and no prompt, the
background watcher **auto-detected it in ~25 s** (FSEvents → stability gate → quiet window →
enqueue → ffmpeg) and joined it end-to-end (**17.4× realtime**, all checks passed). Bonus: the
date/TC discrepancy handler fired correctly — the renamed file's `05-24` filename vs the copied
SRT's `05-21 17:47:15` cue logged `⚠︎ SRT and filename disagree by 196065 s — using SRT first cue`,
exactly the "timecode/SRT authoritative, surface the discrepancy" decision (the mismatch is a test
artifact; real footage agrees).

**Why no prompt is the *answer*, with one honest caveat:** the original 5.14 open question was
"does macOS keep granting the read across relaunch?" — answered **yes**, with zero permission
friction. Because the dialog never visually fired, we can't fully distinguish "powerbox grant
persisted" from "the built-in SDXC slot isn't TCC-gated like a USB SD reader would be." For this
hardware the practical outcome is identical, and the drop-test proves background programmatic reads
work regardless. A USB SD reader (different bus) might still surface the dialog — untested.

## 2. Cleanup

Deleted the synthetic `0099` clip (card + Desktop output), deleted the 6 real joined outputs
(~32 GB freed), quit the watcher, removed the `M4P-1` watch entry from `UserDefaults`, and removed
today's now-orphaned ledger dir. Left the two **older `2CULL` orphan ledger dirs** (26 / 9 groups,
Jun 20) as-is — pre-existing cruft, harmless; flagged that per-folder ledger dirs aren't
garbage-collected when an entry is removed (a low-severity cleanup candidate, not blocking).

## 3. Docs

Flipped **5.14 ✅** and **Wave 5E ✅** in `IMPLEMENTATION_PLAN.md` (Wave 5 fully closed); updated
`PROJECT_STATE.md` Focus/Next/Recent. **Next:** Wave 6 real-footage validation (6.3–6.5), or the
deferred `fix/wave5-watchfolder-hardening` branch (hung-discover / FSEvents-teardown / TOCTOU — the
happy-path pass here does **not** retire those edge cases).

## Resume (pre-clear — next session starts cold)

- ⚠️ **ONE UNPUSHED COMMIT.** `main` is **ahead of origin by 1** — the docs close-out commit
  (docs only, this session). **Push it before any other Mac touches the repo.** Syncthing carries
  the working tree but **not `.git`**, so if the next Mac wakes with these doc files synced while its
  `.git` lacks `3a920eb`, you get exactly the byte-identical Syncthing phantom this session opened
  with. `git push` and it's gone. (Tree is otherwise **clean**; no uncommitted work, no half-done edits.)
- **Pickup point:** **Wave 5 is closed.** Pick **Wave 6** (real-footage 6.3 end-to-end / 6.4 SRT
  alignment / 6.5 variant-guard + edge-cases) **or** the **`fix/wave5-watchfolder-hardening`** branch
  (3 worth-fixing engine-review items: hung-`discover` timeout, FSEvents teardown retain/release,
  enqueue→FFmpeg TOCTOU inode re-verify). **Branch before building** (working agreement).
- **State:** 455 app / 1 skip / 0 fail (no app code changed all session); shipped **1.0.2 / 102**
  untouched; no DMG re-cut needed.
- **Housekeeping (non-blocking):** two orphaned `2CULL` ledger dirs remain under
  `~/Library/Application Support/Conjoyn/WatchFolders/` (per-folder ledgers aren't GC'd on entry
  removal — a low-sev cleanup candidate). A `watchFolder-defaults` backup sits in this session's
  scratchpad (only the discarded `2CULL` entries — safe to lose on clear). A fresh Debug build is in
  DerivedData; rebuild clean next time regardless. The **M4P-1 SD card** is still mounted (watch entry
  removed, so the app won't auto-act on it).
- **Cookbook owed (carried from 2026-06-20):** the removable-volume test gotcha — `diskutil`
  `Removable Media: Removable` vs `Fixed` false-green, **plus** this session's **powerbox finding**
  (`NSOpenPanel` pick suppresses the `NSRemovableVolumesUsageDescription` prompt and the grant
  persists across relaunch) and the `tccutil reset SystemPolicyRemovableVolumes` test technique —
  still wants master cookbook entry **#125** (deferred while the DIRECTIONS master repo wasn't clean).

---

# 2026-06-24 (later) — Watch-folder daemon hardening (the 3 deferred engine-review items)

**Mac:** (this Mac) · **Branch in/out:** `main` → `fix/wave5-watchfolder-hardening` → **`main`** (merged
`--no-ff` `2905b38`, branch deleted) · **Code + tests changed; shipped 1.0.2/102 untouched.**

Closed the 3 worth-fixing items from the 2026-06-23 post-hoc engine review — the hang / use-after-free /
time-of-check edge cases that the 5.14 happy-path eyeball couldn't exercise. Read every load-bearing file
first (the review pointed at line numbers; verified them against current source). One genuine product fork
surfaced (#1 recovery strategy) → asked the user, who chose **timeout + keep cadence** (full self-heal).

- **#2 FSEvents teardown UAF** (`d7e05fe`) — `WatchFolder`'s stream context used `passUnretained` + `nil`
  retain/release, so `stop()`/`deinit` on the owner's actor could free the monitor while a callback was in
  flight on the GCD queue. Gave the stream real **retain/release context callbacks** so it holds its own
  strong ref until `FSEventStreamRelease` (after `Invalidate`) balances it. This is an intentional
  stream↔monitor **cycle** broken by the explicit `stop()` the coordinator always calls — audited the
  teardown (`deactivate → disable → stopMonitor → stop()` on every remove/toggle/re-monitor) to confirm no
  un-stopped drop except at process exit (moot). `deinit` is now a fallback for the unstarted/already-stopped
  case. **Test gotcha learned:** `FSEventStreamRelease` runs the release callback **asynchronously** on the
  dispatch queue, so the dealloc lands a beat after `stop()` — the lifetime tests had to *wait* for it, not
  assert synchronously. That async teardown is precisely what closes the UAF (the stream won't let `self`
  die until its queue drains). +3 lifetime/retain-balance tests.
- **#1 hung-discover deadlock** (`e3f9789`) — `reconcile` latched one `isRescanning` flag cleared by
  `defer`; a `discover()` (ffprobe) that hangs forever never lets the `defer` fire, so the flag latched
  `true` and every later pass dropped → watcher silently dead. **Lesson:** `defer` can't rescue a coroutine
  suspended forever at `await` — it runs on scope *exit*, which never comes. Fix: split into `isDiscovering`
  (heavy rediscovery) + `isResampling` (cheap cadence) so a wedged scan can't freeze the cadence, AND bound
  `discover()` with `withDiscoverTimeout(seconds:)` (new tunable `WatchFolderSettings.discoverTimeout`,
  default 90 s). The wedged task is **abandoned, not awaited** (an ffprobe `Process` ignores cooperative
  cancellation; awaiting would re-deadlock) via a `withCheckedContinuation` race + single-resume `ResumeGate`
  actor. +1 test: a discover that wedges once then recovers proves the latch clears and the watcher enqueues.
- **#4 stale-key cache eviction** (folded into `e3f9789`) — evict `sampleHistory`/`groupLastChanged` for
  clips/groups no longer discovered, bounding memory across a long daemon session over many cards. Pure
  caches only; persisted `groupStates` untouched. Pruned only when a *discovery* confirms a group is gone
  (poll passes reuse `lastGroups`, so a transient read blip evicts nothing).
- **#3 enqueue→join TOCTOU** (`3ee5933`, cookbook #127) — clips captured at enqueue, ffmpeg runs minutes
  later; a card swap / in-camera rotation repoints a `DJI_NNNN` path at different bytes that ffmpeg would
  join silently. New `FileIdentity` snapshots `(device, inode)` via `lstat` at enqueue (in the shared
  `addJob` funnel → manual queue guarded too) and re-verifies before `mergeClips`; `.mismatch`/`.missingNow`
  throws the **non-retriable** `FFmpegError.sourceIdentityChanged`. **Policy call (logged in decisions.md):**
  `.unverifiable` (transient `stat` error ≠ "gone") does *not* block — #127's source was *trashing* (refuse
  on uncertainty is safe there), this is *read-and-produce*, so a momentary read blip shouldn't fail a legit
  job (ffmpeg surfaces a real read error itself). Restored job = no baseline = no-op. Identities transient
  (out of `CodingKeys`). +9 tests (FileIdentity capture/verify + job snapshot/recheck + addJob captures).

**Verification:** full suite **468 / 1 skip / 0 fail** (+13 from 455); clean macOS build. Three commits
(one per fix), merged `--no-ff` to `main`, branch deleted, `main` pushed to origin. `decisions.md` +
`PROJECT_STATE` updated. **Still deferred** (cosmetic / not reachable): unbounded ledger, `nil`-vs-`""`
fingerprint, decorative `WatchGroupState`, shared GCD queue label.
