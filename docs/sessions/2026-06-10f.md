# Session: 2026-06-10f

## Goal
Clear the last two owed items on `feature/rename-tc-disclosure` — **eyeball the per-queue-row
timecode disclosure live on a real card**, then **merge the branch to `main`** and log/update docs.
(Both commits were code-complete + test-green at 220/220 from 2026-06-10d/e, but the panel had
never been seen on real footage and the branch sat 2 commits ahead of `origin`.)

## Progress
- **Clean build cycle** (kill → wipe DerivedData → Debug build → launch). `BUILD SUCCEEDED`, app
  launched (pid 54393). Could not `screencapture` from the shell (no Screen Recording permission for
  the CLI), so the eyeball was user-driven; I supplied the per-line expected values from the source
  (`TimecodeDisclosure.build` + `TimecodeDisclosurePanel`) so the check was correctness, not just
  "it appeared."
- **Card reconnaissance, mid-session correction.** First inspected the *original* test card
  `/Volumes/2CULL/2CULL-IN/DJI_001` — found it's the **2026-03-18 footage with ZERO `.SRT`** (only 22
  LRF proxies). That would have resolved Applied TC **`from filename`** and, crucially, **could not
  fire the slow-mo caption** (slow-mo detection is SRT-span-derived by design → degrades to silent).
  Flagged this to the user before they staged the card. User instead mounted **`/Volumes/M4P-1/DCIM/
  DJI_001`** — the **2026-05-21 SRT-bearing footage** (the same card validated end-to-end in s7/s8) —
  which exercises *all three* panel paths at once.
- **Live eyeball — PASS.** Scanned the card (9 clips, 6 recordings: 2 singles + one `SPLIT·4`
  `0006–0009` + a single `0010`), queued the split's first segment, opened the queue-row caret:
  - **Source TC `—`** ✓ (DJI carries no `tmcd` track — the expected common case).
  - **Applied TC `19:53:03:11`** in orange · **`from SRT cue`** · **`25 fps`** ✓.
  - **Slow-mo caption** fired: *"Slow-mo: timecode starts at the real recording time and advances at
    the file's playback rate (25 fps)."* ✓ — this recording is the known 4× slow-mo (s8).
  - Recordings-hero **split disclosure** (the `SPLIT·4` sublist with `+ …_D.SRT` sidecars) renders
    correctly too.
- **Keystone confirmation.** The Applied TC `19:53:03:11` is **byte-identical** to what the *engine*
  stamped on this exact recording back on **2026-06-09 (s7)** — derived from SRT first cue
  `19:53:03.448` CEST at 25 fps, frame `11` = floor(.448×25). This is the whole premise of Commit 2:
  the disclosure re-runs the **same** `RecordingStartResolver` + `TimecodeFormatter` the join calls
  (`resolveJoinMetadata`), rather than a frozen snapshot — so the readout provably can't drift from
  the stamp. A display build (today) and an engine run (weeks of commits earlier) agreeing to the
  frame is the proof that "one resolver, two callers" beats "compute-once-and-cache."
- **Pushed** the 2 local-ahead commits (`2524b00` Commit 2 + `d5356cf` docs) to
  `origin/feature/rename-tc-disclosure` (`58eb9ce..d5356cf`), then **merged `--no-ff` → `main`**
  (`30c8447`) and **pushed `main`** (`2b72180..30c8447`). main now in sync with origin; feature
  branch retired.
- **Docs:** updated `PROJECT_STATE.md` (retired the stale "not yet eyeballed / not pushed" Next item;
  added the 2026-06-10f Recent entry), wrote this log, indexed it.

## Decisions / notes
- **No code changed this session** — verification + merge only.
- **Slow-mo path caveat:** now eyeballed via the M4P-1 (SRT-bearing) card, but the original
  `2CULL/…/DJI_001` (2026-03-18, no SRTs) structurally *cannot* exercise it. Slow-mo detection is
  best-effort and SRT-derived; a card with no telemetry sidecar shows no caption even for genuine
  slow-mo. Working as designed; recorded so a future session doesn't read its absence as a bug.
- **Cosmetic, unrelated:** the `2CULL/…/DJI_001` card has a Finder-style duplicate
  (`DJI_20260318174442_0009_D.MP4` + `…_0009_D (1).mp4`). Not touched; noted as a possible grouper
  edge case for later.

## Tests
- 220/220 (unchanged — no code touched; the prior `feature/rename-tc-disclosure` suite carried over
  on merge).

## Next
1. **UI polish pass** — enumerate remaining sizing/position deviations vs the prototype against a live
   build (the new TC caret/panel sit in the queue-row region this pass touches). 3 items already done
   2026-06-10d.
2. **Single-file export** (user request 2026-06-10) — let a lone 1-segment recording be copied/remuxed
   so its date/timecode get stamped + `.SRT` carried over (engine currently refuses <2 segments).
3. **DMG wrapper** for distribution.
4. Smaller polish: Apple `Keys` creationdate atom (6.3); doubled camera-variant suffix
   (`…_0009_D_D.mp4`).
