# Session: 2026-06-11g

## Goal
Complete the help window, clear remaining eyeball items, confirm ship-readiness.

## Progress

- **fps display fix** — `r_frame_rate` (codec-signaled) now preferred over `avg_frame_rate`
  (timestamp-computed, can drift to 29.97 on genuine 25fps DJI clips). `rFrameRate` added as optional
  field on `VideoStreamParams`; grouping guard updated to match. +2 tests → 319/319.
- **Recording visibility filter** — All/None/Splits/Singles buttons now show/hide rows (not just
  toggle selection). `RecordingFilter` enum + `filteredGroups` computed property on VM. Active filter
  highlighted orange (`Theme.acc2`). `setFilter()` auto-selects matching rows; `resetFilter()` called
  on scan. Live-confirmed: 60 singles / 14 splits of 74 total, correct counts and visibility.
- **Help window shipped** — Vendored AppHelp as local Swift package `01_Project/HelpMenu`.
  9 markdown topics (What it does / Quick Start / Scanning / Singles+Splits / Dates+Timecode /
  Queue / Joining+Verification / Keyboard Shortcuts / Troubleshooting). `HelpMenuCommands` wired
  into `.commands`; manifest-loaded from app bundle. `AppDelegate` patches the NSMenuItem to show
  `⌘?`. `?` toolbar button added next to Scan (`questionmark.circle`). All 319/319 tests pass.
- **SRT mismatch eyeball** — Created test fixture at `/tmp/srt-mismatch-test/` (copied 10 MB clip
  `0043_D`, shifted SRT first-cue timestamp +20 min). Chip fired: "SRT/filename differ by 20 min"
  in orange; Applied TC correctly used SRT cue. ✓
- **Slow-mo chip** — confirmed live in earlier eyeball session (clips `0055/0056/0057_D`). ✓

## Decisions
- No new architectural decisions. Help content lives in `01_Project/Conjoyn/Help/` as markdown
  files loaded via a JSON manifest — content editable without code changes.
- `AppDelegate.applicationDidFinishLaunching` patches the help NSMenuItem's `keyEquivalent` from
  `"/"` (SwiftUI's translation) back to `"?"` so the menu shows `⌘?`. Functional shortcut is
  `Cmd+Shift+?`; `Cmd+?` (bare, without Shift) does not fire — keyboard layout issue, not pursued.

## Next
- **DMG wrapper** — last remaining ship task.
- **Screenshots for release docs, website, and help content** — need a list of required shots:
  - App in Empty state
  - App in Loaded state (recordings list with a mix of singles + splits)
  - App with slow-mo chip visible
  - App with SRT-mismatch chip visible
  - Queue with jobs running / ETA visible
  - Queue row disclosure (TC disclosure panel)
  - Green seal on a completed job
  - Help window open
  - (For website) hero shot — Loaded state, full window, clean card
- **Website** — needs copy, screenshots, download link once DMG exists.
- **Help content images** — optional but would improve the help topics.
