# 2026-06-16 — Doc reconcile: log the AppCitizenshipKit migration

## Context
`/status` flagged that `main` carried **two commits newer than the last session log**
(2026-06-14), both adopting **AppCitizenshipKit (ACK)** — and PROJECT_STATE still described the
older split FeedbackKit + Donate wiring. Git itself was already clean (`main == origin/main`,
0/0 ahead/behind, tree clean), so nothing to merge/pull/push; the only drift was **documentary**.
This session catches the record up.

## What actually landed (both commits dated 2026-06-14, after that day's log was written)

- **`8d8ccad` — Adopt AppCitizenshipKit for Feedback + Tip Jar + About.** Replaced the
  hand-assembled menu wiring with a single `CitizenshipCommands(citizenship)`:
  - `ConjoynApp.swift`: `FeedbackCommands` + the local `DonateCommands` struct collapse into one
    `CitizenshipCommands(citizenship)`; `import FeedbackKit` → `import AppCitizenshipKit`. Adds a
    **link-rich About panel** (replacing the stock one), coexisting with "Check for Updates…".
  - `CitizenshipConfig(appID: "conjoyn", appName: "Conjoyn", accent: Theme.acc2,
    websiteURL: …/conjoyn, privacyURL: …/privacy, logProvider: DiagnosticLogger.recentTail(80))`.
  - `project.yml`: **drop the direct FeedbackKit dep** (now transitive via ACK), add
    `AppCitizenshipKit` (`from 0.1.1`).
  - Help topic + menu reworded **"Donate"/"Support" → "Leave a Tip"** (tip-jar framing — not
    charity, not help-desk "Support"). `help-donate.md` + `help-manifest.json` touched.
  - Net `ConjoynApp.swift` is *smaller* (52 lines changed, −11 net): the one-liner replaces the
    bespoke struct.

- **`9c216ea` — Fix stale "Support Conjoyn" comments; bump ACK 0.1.1 → 0.1.2.** Doc comments in
  `ConjoynApp.swift` still said "Support Conjoyn" after the label was finalized as "Leave a Tip";
  corrected to match the code. ACK bumped to **0.1.2** (its matching comment-only fix). No behavior
  change.

## Why this matters
ACK was **generalized from Conjoyn's own** Feedback (#102) + Tip-Jar (#104) patterns, then adopted
back here — dogfooding the package. FeedbackKit still does the work (ACK re-exports it; server
still gates on `ALLOWED_APPS`, which already allow-lists `conjoyn`); the change is *packaging*, not
behavior. The Feedback↔Tip divider is now emitted by `CitizenshipCommands` itself; only the
Help↔Feedback divider remains a manual `CommandGroup(after: .help) { Divider() }` (cookbook #104,
same-anchor groups order by declaration).

## State unchanged by this
- **Version still `1.0.1` / build `101`** — the migration added/changed no version.
- **Still 100% feature-complete**; the one gate to 1.0-public is still **Sparkle Wave 4**
  (website, in the `3-Websites/App-Websites` repo — memory `wave4-lives-in-websites-repo`).
- **Ship artifact still lags `main`** — `04_Exports/Conjoyn.dmg` is `1.0/build 100`; the DMG re-cut
  owed before any public ship now *also* owes this ACK change (memory
  `dmg-recut-on-fresh-release-mac`).

## Owed / next
- Eyeball the live ACK menu surfaces (Send Feedback… / Leave a Tip / About panel) on a fresh build.
- DMG re-cut from current `main` before public ship.
- Wave 4 (website) — the only real gate.
- No engine/test change; tests last green at **341 app / 1 skip / 0 fail**.
