# Session: 2026-06-10k

## Goal
Live-test single-file (N=1) export on real footage and merge it; then act on live-UI
requests (Singles filter, TC-source question, output-folder-vs-queue UX trap).

## Progress
- **Single-file export — live-tested + merged (`73cffed`).** Clean build cycle (229/229
  tests, 1 known skip; both single-file tests ran incl. `testSingleFileExportStampsTimecode`
  with real ffmpeg). **Independent real-footage verification** on clip `0004`
  (`/Volumes/M4P-1/DCIM/DJI_001`, 0.98 GB, 4 streams) using the *bundled* ffmpeg + the exact
  production arg vector from `buildMergeArguments` (N=1): **source v:0 packet MD5 == output
  v:0 MD5** (`83d00edc…` — byte-identical, truly lossless), 5554→5554 frames, mjpeg 1280×720
  preview + 2 telemetry data tracks dropped (output = 1 hevc + a fresh `tmcd`), duration
  222.16 s exact, `creation_time=2026-05-21T17:47:15Z` + `tmcd=19:47:15:08` stamped (frame =
  floor(0.349×25fps), from the SRT first cue), faststart (moov @ 36 before mdat @ 32599).
  Merged `--no-ff` → `main`, pushed, branch deleted. GUI eyeball (tick lone row → Start) still
  owed — no UI automation available to me.
- **"Singles" selection filter — merged (`cee27e3`).** Mirror of the existing "Splits"
  quick-filter: new `selectSingleGroupsOnly()` on the ViewModel (filters `groupType == .single`),
  added as a 4th `CJSegGroup` action → bar is now **All · None · Splits · Singles**
  (`RecordingsList.swift:41-46`, `ConversionViewModel.swift:84-86`). Build-verified, branched
  (`feature/singles-filter`), merged `--no-ff`, pushed, branch deleted.
- **TC-source question — investigated, NO change (user confirmed "keep filename priority").**
  User saw `Applied TC 17:42:55:00 · from filename` on a no-SRT clip and proposed switching to
  file creation time as "more precise." Traced `RecordingStartResolver` (priority: manual → SRT
  first cue → **filename** → embedded creation_time → filesystem). Showed the label is accurate
  (value parsed from `DJI_20260318174255`), and filename is deliberately ranked **above**
  creation_time because: filesystem dates reset on card→disk copy; the embedded atom is UTC/often
  skewed (proof: clip `0004` filename `19:47:15` CEST vs atom `17:47:15Z` — the 2h offset);
  filename is timezone-independent camera-local time, which is what a time-of-day TC wants. And
  no precision gain — filename + atom are both second-resolution (`:00` frames); only the SRT
  cue is sub-second, and this 2026-03-18 card has no SRTs.
- **New backlog items logged in PROJECT_STATE:** **(5b)** codec + resolution (+fps) in the
  recordings-row empty horizontal space (data already in hand via `DJIClip.streamInfo?.video`);
  **(9)** per-recording manual TC entry (`HH:MM:SS:FF` overriding the stamped `tmcd`).
- **Output-folder-vs-queue UX trap — diagnosed, A+B plan approved (next session).** User added
  jobs, then changed the Output folder expecting it to apply to the queue; it didn't, with no
  feedback. Root cause: `addToQueue()` (`ConversionViewModel.swift:177-187`) **freezes**
  `outputFolderURL` into each `job.destinationURL` at enqueue; the Output bar governs only
  *future* additions, and **queue rows never show their destination** → the divergence is
  invisible. Freezing per-job is correct for a queue; the real defect is missing feedback.

## Decisions
- **Filename stays ranked above creation_time in `RecordingStartResolver`** (reaffirmed). It's
  camera-local + survives a card→disk copy; creation_time is UTC/skewed, filesystem resets on
  copy, and neither improves on filename's `:00` frames. The no-SRT `:00`-frame "imprecision" is
  inherent, not a resolver bug. (Already in PROJECT_STATE; see also `decisions.md` 2026-06-09 s6.)
- **Output destination is frozen per-job at enqueue (kept), fix the feedback instead.** Approved
  **A+B** for next session (see Next).

## Next
- **Implement A+B for the output-folder-vs-queue trap** (UI-changes protocol: propose exact
  placement before coding):
  - **Tier A — Transparency:** show each job's destination folder in the queue (inline dim
    `→ <folder>/` after the name, or in the existing row-disclosure panel beside the TC info).
    Data is on `job.destinationURL`. Pure display, low risk.
  - **Tier B — Re-apply on change:** when the Output folder changes *and* `.pending` jobs exist,
    prompt **"Apply new output folder to N pending jobs? [Apply / Keep current]."** Only
    pending/unstarted jobs (`!status.isFinished`, not `active`/`preparing`) get rewritten —
    recompute `newFolder + same stem`, re-run collision resolution. Never touch active/Done jobs.
  - Tier C (pending jobs track the live Output folder, freeze only at start) noted as the bigger
    optional model change — touches rename/collision logic; defer unless A+B prove insufficient.
- Standing backlog (PROJECT_STATE "Next"): (4) ETA readout · (5)/(5b) empty-space integrity +
  codec/dimensions · (6) wire source↔target verification · (7) Help window · (8) DMG wrapper ·
  (9) manual TC entry · small polish (6.3 Apple `Keys` atom, doubled variant suffix).
- Owed: GUI eyeball of single-file export (tick a lone row → Start) on a real card.
