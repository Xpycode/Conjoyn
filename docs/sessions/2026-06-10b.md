# Session 2026-06-10b — GUI design pass: port the handoff to SwiftUI

## Goal
Execute the queued GUI design session: port `02_Design/design_handoff_conjoyn/` to SwiftUI
(restructure the main window to the designed vertical flow), then continue into the
rename-popover / TC-disclosure spec and the DMG wrapper.

## Progress
- **Ported the full design handoff to SwiftUI** (`design/ui-port` → `main`, `5057f14`). The
  main window is now the designed single vertical flow — titlebar/source bar → discovered
  recordings (hero) → output settings bar → queue → collapsible console → footer — replacing
  the old two-pane HSplitView layout.
  - `Theme.swift` rebuilt as the 1:1 token set from `styles.css` (surfaces, hairlines,
    txt/txt-2/txt-3, `acc1` #FFB23E / `acc2` #F0622A, ok/bad).
  - New `Views/DesignControls.swift` (button styles, segmented action group, orange checkbox,
    SPLIT/SINGLE badge, 5 pt progress bar with deliberately-unanimated width, path wells,
    section header, formatters), `Views/RecordingsList.swift` (row spec: checkbox, disclosure +
    monospace segment sublist, **real first-frame thumbnails** via `ThumbnailManager`, en-dash
    range titles, date·time sub-line, tabular meta columns, 12 % selection tint, hover; empty
    state with **drag-and-drop of a folder**; scanning state), `Views/QueuePanel.swift`
    (output bar, queue rows, console with line tinting + auto-scroll, footer with Start/Stop
    swap and green all-done totals).
  - Window: `.hiddenTitleBar` + 52 pt unified custom titlebar (traffic-light inset, Penumbra
    app-shell pattern), 1240×800 default / 1000×640 min, background-drag enabled.
  - Engine knobs that didn't make the design bar (container, filename, re-encode,
    delete-after-verify) moved to a **gear popover** — nothing lost.
  - Behavior per handoff: Add-to-Queue now **skips groups with an unfinished job already
    queued** and **clears the selection** after adding.
  - Relabeled **"Preserve timecode" → "Timecode from recording time"** (the spec Part 2
    relabel; binding unchanged).
- **Build clean, 195/195 tests pass.**
- **User live-tested on the real card mid-session** (DJI_001, 110 clips → 74 groups):
  scan, split disclosure sublist, options popover, and a **real join driven through the new
  UI — 1/1 joined, green Done state**. Screenshots confirm all five design states render.
- **Live test surfaced a gap:** queueing a *single* recording fails at processing with
  `Invalid input: Need at least two segments to join (got 1)` — the queue accepts 1-segment
  jobs but the join engine refuses them. Folded into the feature request below.
- Started `feature/rename-tc-disclosure` (research only: mapped
  `RecordingStartResolver`/`TimecodeFormatter`/`QueueManager.addJob` APIs, identified
  Penumbra's `SourceTimecodeReader` for the tmcd port, sketched the pattern engine + collision
  rules). **No feature code yet** — branch exists with no commits.

## Decisions Made
- **Design-bar scope (minor, UI-only):** the output bar carries only the handoff's three core
  switches; all other engine knobs live behind a gear popover. Reversible presentation choice —
  not promoted to `decisions.md`.

## Feedback / Requests (user, end of session)
- "Some sizing and position issues, but looks good" — a **polish pass against the prototype**
  (spacings, control sizes, positions) is wanted.
- **New feature request: single-file export.** While we're a joiner, a 1-segment recording
  should still be exportable so its date/timecode get stamped (and its `.SRT` carried over) —
  i.e. a single-input copy/remux path with the same metadata stamp, instead of today's
  "need at least two segments" failure.

## Next Session
1. **UI polish pass** — fix the sizing/position deviations vs the prototype (compare side by
   side; the user has concrete spots in mind).
2. **Spec + implement single-file export** (stamp TC/date on a lone clip via copy/remux; relax
   the ≥2-segment guard; decide queue-row presentation).
3. **Continue `feature/rename-tc-disclosure`** — Rename Joined Files popover (spec Part 1) and
   per-job TC disclosure caret (Part 2); research is done, implementation pending.
4. DMG distribution wrapper (still queued from 6.2).
5. Polish backlog: Apple `Keys` creationdate atom (6.3), doubled variant suffix (`…_D_D.mp4`).

## Branches
- `design/ui-port` → merged to `main` (`5057f14`).
- `feature/rename-tc-disclosure` — created, no commits yet.
